# Owner-reported issues — live tracker

Owner order 2026-08-23: "keep track of all the issues I am mentioning and
take upon them". Restated harder 2026-08-25: "everything I tell you, I
need you to consider it and have a place for it, and never skip it".

**The rule this file exists to enforce:** every owner message gets a row
HERE FIRST, before any work starts on it — including asks that sound like
passing remarks, and asks that get answered rather than built. An ask that
lives only in a session transcript is an ask that dies at the next
compaction. This file stalled at #14 for two days while a dozen asks
accumulated in conversation only; that is the failure mode.

A row is never deleted, only re-statused. One row per report, newest
first. Status: OPEN / IN-PROGRESS / FIXED-verified / FIXED-unverified /
ANSWERED / DEFERRED-by-owner.

| # | Reported | Issue (owner's words, compressed) | Status | Evidence / next step |
|---|----------|-----------------------------------|--------|----------------------|
| 32 | 08-25 | "switched to VPN so my network traffic doesn't get bloated with YouTube recommendations" | NOTED | Context only, no action. Worth knowing when reading any latency or request measurement taken from the owner's side today. |
| 31 | 08-25 | "Always recheck before considering something done. Visual rechecking is what I want, not just thinking and assumption." | STANDING RULE | Earned the hard way the same hour it was given: every DOM probe said the end-screen was hidden (`display:none`, 0x0, on the exact selector the rule targets) and a screenshot showed twelve recommendation cards over the finished video. YouTube had reskinned the surface — the legacy element our rule hides still exists and still measures hidden, while the real cards render in new markup. A computed style proves a RULE fired; only a pixel proves the SURFACE is covered. Rule: for any claim about something visible, capture the image and look at it before saying done. Written into the gauntlet skill and this file. |
| 30 | 08-25 | "everything I tell you, I need you to consider it and have a place for it, and never skip it" | DONE | This file is the place. Backfilled rows 15-29 for every ask made 08-24/25 that had been living in conversation only. Rule written into the header: a row is created BEFORE work starts, and never deleted, only re-statused. |
| 29 | 08-25 | "only keep one cron" | DONE | Deleted the every-4-min worklist ping (`0a221190`). Single remaining job: `c65acaf7`, gauntlet round, every 6 min. |
| 28 | 08-25 | "hide after video ends the cards that come up ie recommendations etc. also blur them as well in case someone has them enabled" | IN-PROGRESS (reopened by owner screenshot) | FIRST ATTEMPT WAS WRONG and every DOM probe agreed with it. Hid the legacy `.ytp-endscreen-content` / `.ytp-videowall-still`; those elements still exist (count 1 and 12) and measure `display:none` at 0x0, so the rule read as working. The owner then screenshotted twelve cards over his finished video. YouTube has reskinned the end screen: the live markup is `.ytp-fullscreen-grid` > `.ytp-fullscreen-grid-stills-container` > `.ytp-modern-videowall-still` (12, 300x273) with `.ytp-modern-videowall-still-image` (300x169), and the player carries `.ended-mode .ytp-fullscreen-grid-active`. Added both new selectors to the HIDE layer (rules/youtube.txt) and the modern trio to the BLUR layer (rules/blur/youtube.css). Verified `grid.contains(video) === false` before hiding, so the player red line holds. Rebuild + visual recheck in progress — this row does not move to FIXED on a computed style again. |
| 27 | 08-25 | "you watch it, i don't want to see women" | STANDING RULE | All gauntlet frame inspection is done by me. Reports carry counts and file names only; frame images are never surfaced to the owner. Written into the gauntlet skill. |
| 26 | 08-25 | "i signed out of yt so my channel doesn't get filled with trash" | STANDING RULE | Never sign in; never pollute watch history. Gauntlet resolves video ids via live search on a signed-out session. |
| 25 | 08-25 | "you don't have to build new apk each time, you can just use the desktop app" | CONFIRMED — adopted | Correct, and it is now the loop's default: the gaze bundle is identical on desktop and Android, so blur logic is iterated on the desktop dev app over CDP and an APK is cut only when a change is ready for the phone. Rust/Kotlin changes still need a build. |
| 24 | 08-25 | "why doesn't gauntlet ping you? the loop doesn't work at all" / "cron does not fire" | FIXED-verified | Not the cron expression, which was correct at four different intervals. A scheduled ping only fires while the session is IDLE, and this session held a TRACKED background task (`npx tauri dev`) for 13 hours plus an `until...sleep` wait-loop for 12h39m — so it never once reached idle. Killed both, relaunched the dev app DETACHED via Start-Process; both crons fired immediately after. Rule recorded in WORKLIST.md and the gauntlet skill. |
| 23 | 08-25 | "ads started appearing again, which was unprecedented... ads are the biggest issue we have. Why were they able to get through?" | IN-PROGRESS | Root cause, and it was never a regression: the adblock engine has shipped since Phase 2.5 with EasyList/EasyPrivacy/uBO compiled in, but the code only ever called `url_cosmetic_resources`. Cosmetic filtering hides ELEMENTS; it cannot stop a REQUEST, and a pre-roll is not an element. Network blocking was never wired at all. Now: `blocks_request()` in lib.rs (cargo 35/35, tested both directions incl. that the watch document, /youtubei/v1/player, i.ytimg, googlevideo and base.js are NOT blocked), WebView2 interception on desktop (VERIFIED: 7 ad requests answered 204, zero through, player still plays), JNI `nativeShouldBlock` + a delegating WebViewClient on Android. PRE-ROLL VIDEO needed a second mechanism — its stream is googlevideo.com, same origin as real content, so no URL rule can touch it: new `trusted-prune-window-json` scriptlet strips `adSlots`/`adPlacements`/`playerAds` out of `ytInitialPlayerResponse` on the way IN, before YouTube's own script reads it. REMAINING: build the APK and verify all four measured URLs blocked on-device with the player still playing. Not done = not fixed. |
| 22 | 08-25 | "cartoon films are not the biggest concern, so do that later" | DEFERRED-by-owner | Animated/cartoon faces are a known detector gap (BlazeFace + faceres are trained on photographic faces). Explicitly deferred by the owner; do not spend gauntlet rounds on it until he lifts this. |
| 21 | 08-25 | "be sure to make it optimized and performance oriented as well. That is the only way this app would be helpful" | IN-PROGRESS (standing) | Promoted to a first-class, per-round measured constraint rather than a footnote: every gauntlet round records verdict/position pass p50/p95/max, and a fix that buys accuracy at the cost of the pass tail is judged on both numbers. Target hardware is the Helio G88, not this desktop. Current tail is the open item — see GOAL.md ROUNDS. |
| 20 | 08-25 | "make an instance where the detection tells you there are no humans in the frame and you don't have the blur — the opposite detection" | FIXED (owner's own idea, adopted) | Implemented as an eraser rather than a suppressor: `wipeIfEmpty(tracks, personCount, faceCount)` drops every track when a verdict pass sees zero persons AND zero faces, and coast time for a blurred track was cut 3000ms -> 900ms so a patch cannot outlive its subject. This is the GHOST class in the gauntlet. |
| 19 | 08-25 | "there shouldn't be any frame that is being blurred which has no human at all" | IN-PROGRESS | Scored every round as the GHOST class. R3 proved the "random floating boxes" complaint was NOT ghosts but DUPLICATE patches (two tracks per person, drawn stacked with a seam) — fixed via containment-based merge + dedupeObservations. Still scored every round. |
| 18 | 08-25 | "for especially women, blur them fully and not leave anything — the legs or the hands or the head, like bigger blur box" | FIXED-verified (desktop) | Patch geometry reversed to WHOLE BODY: all 17 MoveNet keypoints extend the patch (legs included), keypoint margin 0.03 -> 0.05, new outward PATCH_MARGIN 0.08, personFromFace widened to a full body. A hip clamp added mid-session was REVERTED as contradicting this ask. Evidence: R1 f004. |
| 17 | 08-25 | "not a single frame that the other gender is visible and not a single frame where the wrong gender is blurred up" | IN-PROGRESS (the bar) | Written verbatim at the top of spikes/gauntlet/GOAL.md and decomposed into five terminal per-frame failure classes: EXPOSURE, PARTIAL, FALSE COVER, GHOST, DRIFT. Every round scores all five in both gender directions. |
| 16 | 08-25 | "can you go on a loop... multiple videos... frame by frame analysis... spawn a critic agent that critiques you... test both ways for male and female... an overnight run... a gauntlet loop" | BUILT | spikes/gauntlet/{gauntlet.py,GOAL.md,lock.py} + .claude/skills/gauntlet-round. One round = capture -> score -> critique -> fix -> re-verify -> log, on a rotation of 8 query/gender pairs, ids resolved live. Paired blur-on/blur-off capture (owner's idea) makes scoring objective. 90-min self-expiring lock stops two rounds racing one checkout. R0-R3 landed real fixes; ROUNDS log in GOAL.md. |
| 15 | 08-24 | "you did not add the Falcon icon on the web page. Set an agent to do it" -> later "just use the spiral thing" | DONE (not deployed) | There is no falcon in the brand kit; the mark is a spiral, confirmed by the owner. Spiral mark is in web/index.html. NOT live: tamescroll.com still serves a copy predating the brand commit, and a redeploy is outward-facing — needs an explicit OK. |
| 14 | 08-23 | "blur shows above the menu or a title" | FIXED-verified (emulator) | Region overlays (max z-index, document-anchored) punched over m.youtube's position:fixed topbar when a face thumbnail scrolled behind it. clampToInset() clips overlay top to the header line (fully-behind hide; over-blur preserved). topInset() walks each elementsFromPoint hit's ANCESTOR chain — the top-center hit is a static <button> inside the fixed topbar, so direct-hit-only found inset 0. probe48: barBottom 48, punchThrough []; screenshot shows search bar clean. Shipped v0.1.3 (1003). |
| 13 | 08-23 | Stop re-sending APK via WhatsApp — better delivery to remote phone | DONE (verified) | In-app updater live end-to-end (probe46): emulator v1000 checked live GitHub manifest -> saw v1001 -> card -> downloaded 45MB -> sha256 verified -> FileProvider -> system installer consent prompt fired. Release published (app-v0.1.1, arm64). APK stripped 329->45MB. Bootstrap: owner installs current arm64 from Files ONCE (has updater); future updates in-app. Next-build flow exercised for v0.1.3 (issue #14 fix): bump 1003, strip 45MB, gh release v0.1.3, manifest pushed. |
| 12 | 08-23 | "why did ad come up" (desktop watch) | FIX-SHIPPED (v1004, phone-verify) | Root cause + 3 options written to docs/scriptlet-gap.md (recommend request-shaper-only). Player-red-line work -> owner-grill, not autonomous. Same root as #9. |
| 11 | 08-23 | Sign into YouTube with device accounts instantly | ANSWERED | Not feasible: WebView sandboxed from device Google accounts; cookies persist so sign-in is once per device. Optional: verify Android password autofill in WebView. |
| 10 | 08-23 | Blue selection box on search click + general unpolish | FIXED-verified (emulator) | styles.css: tap-highlight killed, focus ring, user-select, autofill tint, overscroll, drag ghosts. probe49c: add-platform input focused+typed — tapHighlight transparent, outline 0px, border #343434 (clean subtle field, no blue box); no-circumvention 'We don't support that' intact. Shipped v0.1.3. |
| 9 | 08-22/23 | App "loads a lot" after clicking a video | FIX-SHIPPED (v1004, phone-verify) | Dominant cause = #12 scriptlet stall (docs/scriptlet-gap.md). Secondary safe levers listed there: WEBGL_USE_SHAPES_UNIFORMS SHIPPED (benched on real Android WebView: gender shader compiles 223->98, per-new-batch recompiles 68->12 at batch5, output bit-identical). 3x bundle eval remains. region-blur heartbeat thrash FIXED (probe-guard, 1 read/tick when static + skip when hidden; gaze 32/32). |
| 8 | 08-22 | In-video live blur must work on mobile, perf "very well optimized" | FIX-SHIPPED (v1005, phone-verify) | Batched gender (N faces -> 1 inference) + single-download JS NMS shipped; desktop true cost 19.6ms/frame face, 17.1ms NSFW (dataSync bench). Emulator x86_64 inference minutes-slow (GPU emulation, known) — real-hw verify needs owner phone. |
| 7 | 08-22 | Both genders face-blurred even with "male" selected | FIXED-verified (emulator) | Root cause: embedded gender-ssrnet-imdb broken (single saturated output; data[1]=undefined -> every face flagged). Replaced with Oarriaga gender.json (MIT). probe44 in-app man mode: Trump (clear male) CLEARED/sharp, obscured faces region-blurred (0.85 fail-safe) — differentiated verdicts the old model couldn't produce. Real-hw timing still owner-phone. NOTE: 0.85 over-blurs obscured/low-confidence male faces (MrBallen) by design. |
| 6 | 08-22 | In-video blur toggle wanted | FIXED-verified | Pill inside #movie_player, desktop+emulator verified (pre-compaction). |
| 5 | 08-22 | Blur boxes "where they don't even belong" | FIXED-verified | Region overlays document-anchored + 250ms heartbeat + snap guard (probe32/33). Watch for recurrence. |
| 4 | 08-22 | Pinch-to-zoom in fullscreen | FIXED-verified | ScaleGestureDetector 1-3x, reset per enter/exit (probe41). |
| 3 | 08-22 | Fullscreen doesn't actually fullscreen | FIXED-verified | Delegating WebChromeClient + immersive landscape (probe41). |
| 2 | 08-22 | OTA - "will I have to redownload?" | ANSWERED | Rules OTA only; code/scriptlets/models need new APK. |
| 1 | 08-22 | "Lot of loading" (first report) | IN-PROGRESS | Superseded by #9 breakdown. |

Perf findings backlog (from 08-23 profiling agent, not owner-visible yet):
- Scriptlet gap: json-prune-xhr-response, trusted-json-edit-xhr-request,
  rpnt/trusted-rpnt (serverContract), nano-stb, trusted-prevent-dom-bypass,
  rmnt — skipped in rules.rs as "anti-detection", actually load-bearing for
  watch-click latency (4.4s) and hard-nav pre-rolls.
- off-mode kickNsfw() immediate -> 2.2s main-thread webgl compile during
  page load; consider idle-deferred backend warm without violating pre-blur.
- Bundle eval'd 3x per hard nav (Started+Finished+unknown 3rd) ~200ms each.
- region-blur heartbeat forced layout 146ms/15s (clientWidth reads).
- Bench gotcha: hidden-tab timer throttling makes async tfjs downloads
  look 35-3000x slower than real; bench only in visible tabs or dataSync.
