# Critic ledger

Every finding filed by a critic run lands here as one row, verdict `OPEN`.
A row closes only with an artifact, never with an argument
(`docs/critic-loop.md` §7):

- `CONFIRMED` — a commit that fixes it, plus a check red before and green after.
- `REFUTED` — the falsifier was **run**, and its raw output is pasted into the row.
- `ACCEPTED-COST` — cite the `docs/engine-findings.md` §9 line that makes this a cost.

`bench/critic-gate.mjs` exits non-zero while any `EXPOSURE` or `WRONG-NUMBER` row
is `OPEN`.

| id | date | trigger | severity | claim | falsifier | verdict | resolution | evidence |
|---|---|---|---|---|---|---|---|---|
| A1 | 2026-09-02 | phase-a (T1/T3) | EXPOSURE | `cut-truth.mjs:74` pairs an scdet cut at time `t` with the 10Hz delta at `ceil(t*10)`; correct is `round(t*10)`, so ~half of "cuts" read the post-cut steady frame (p05 at a cut = 0.0). Every recall figure in `scene-gate.mjs`, `tuning.mjs` and `scene-gate.test.mjs` is wrong, and the knee inverts: correctly aligned, recall halves between **60 and 75** (92.8% -> 50.0%), not between 75 and 90. The OTA ceiling of 75 and the written "75 is where to go next" rest on the wrong table. | `cd app/gaze/bench && SCDET=30 node cut-truth.mjs` reproduces the shipped table; change line 74 to `Math.round(t * RATE)` and re-run — recall @60 goes 45.5% -> 95.9%. `SCDET=25` with `round`: @60 92.8%, @75 50.0%. | CONFIRMED | Pairing corrected to round(t*RATE) and the instrument now SWEEPS the offset and throws unless 0 wins (25x separation: p50 59.8 vs 1.7/2.4). 10g retracted wholesale. **But F1 concluded the wrong thing from a right finding:** it read the recall cliff as an exposure risk at 75, and the labelled corpus -- which measures exposure directly -- has it FALLING 67.0 -> 57.0 across that step. Ceiling stays 75; the real cost there is phantom. See 10j/10k. | `docs/critic/phase-a-2026-09-02.md` F1 |
| A2 | 2026-09-02 | phase-a (T2) | WRONG-NUMBER | `cut-truth.mjs` ships `SCDET` default 8; the committed table only reproduces at `SCDET=30`, which is recorded nowhere. At the committed default the gate catches 11.3% of cuts at 60, not 45.5% — below the 26% line `scene-gate.test.mjs` itself calls "not a gate". `SCDET=30` also labels only 123 cut frames over 4.2h of ten videos. | `cd app/gaze/bench && node cut-truth.mjs \| tail -12` vs `SCDET=30 node cut-truth.mjs \| tail -12` | CONFIRMED | SCDET is a swept axis now, not a hidden default. Corrected table at every ground truth in 10j; at a HARD-cut population (scdet>=25/30) the gate catches 92.8-95.9% at 60, not 45.5%. | `docs/critic/phase-a-2026-09-02.md` F2 |
| A3 | 2026-09-02 | phase-a (T2/T3) | WRONG-NUMBER | The birth-rung benefit quoted in `person-track.mjs` (-38.0s false cover, "near-zero exposure cost") is a `CUT_DELTA` **50** number; 1088 ships **60**, where it measures -30.5s false cover and **+5.0s exposure** in man mode. "False cover roughly HALVES" (`person-track.test.mjs:1517`) is false at every bank: -15.7% at 60, -18.5% at 50, -25.3% at 28. | Back up `Z:/tamescroll-corpus/bank/cuts.json`, `cp bank/cuts-60.json bank/cuts.json`, `cd app/gaze/bench && node birth-ab.mjs`, restore. Expect +5.0s / -30.5s at k=3. | CONFIRMED | Re-measured at the shipped bank: **+5.0s exposure, -30.5s false cover** (k=3 man). "roughly HALVES at near-zero exposure cost" removed from person-track.mjs and person-track.test.mjs. Root cause was a LITERAL `CUT_DELTA 50` in the arm label; it reads `cutBankDelta()` from the bank stamp now. | `docs/critic/phase-a-2026-09-02.md` F3 |
| A4 | 2026-09-02 | phase-a (T3) | DEAD-CHECK | `assertCutsFresh` runs from the top-level `const ARM = makeArms(SHIPPED)` at `arch-arms.mjs:570`, so it throws during module evaluation. `bank/cuts.json` is stamped 50 against a bundle shipping 60, so **every** corpus arm — `cut-sweep`, `birth-ab`, `births`, `iou-ab`, `bar-ab`, `closeup-ab`, `grace-ab`, `instant-ab`, `memsignal-ab`, `sim-ab`, `trust-ab` — fails to start at HEAD. `setCutBank` cannot rescue it: the throw precedes any consumer statement. | `cd app/gaze/bench && node cut-sweep.mjs ; echo "exit $?"` and `node births.mjs man ; echo "exit $?"` — both non-zero at HEAD. | CONFIRMED | Arms are lazy: importing arch-arms.mjs is side-effect-free, first USE still asserts. Default bank re-derived at 60, and `corpus-cuts.mjs` now self-names any non-shipped delta so a sweep cannot clobber it. `cut-sweep` is stamp-driven. All arms start; cut-sweep and birth-ab tables in 10j/10k are from the fixed harness. | `docs/critic/phase-a-2026-09-02.md` F4 |
| A5 | 2026-09-02 | phase-a (T3) | DEAD-CHECK | `resetPersonSkip` is imported into `init-entry.js` and never called; its body is tree-shaken out of the emitted bundle. The skip state moved from a per-`attachVideo` closure to module-global, so once `PERSON_SKIP_EVERY` is pushed above 1 a backed-off state survives an SPA navigation, and the player shares one counter with feed previews. The test pinning the reset calls the function directly, so it passes against code no production path runs. | `grep -c 'resetPersonSkip' app/gaze/src/init-entry.js` = 1 (the import). `node -e "const s=require('fs').readFileSync('app/src-tauri/gaze-page.js','utf8');const i=s.indexOf('function c2(');console.log(s.slice(i,i+220))"` — the module emits only the setter, `wantPersons`, `notePersons`. | CONFIRMED | `resetPersonSkip()` wired into init-entry loadstart beside the other per-stream resets. Two tests, the first asserting on the loadstart handler SOURCE because a direct-call test is exactly what passed against the dead import; proved red before green (1 fail -> 0). | `docs/critic/phase-a-2026-09-02.md` F5 |
| A6 | 2026-09-02 | phase-a (T2) | WRONG-NUMBER | ffmpeg `scdet` is not an independent instrument: it scores the luma plane only (a red->green hard cut at constant luma scores 0.391), and `pearson(our 16x16 luma delta, scdet score) = 0.498` over 152,376 frames. It shares the blind spot the comment attributes to our gate alone, so it cannot measure how many similarly-lit cuts we miss. | Build the two-colour clip with `ffmpeg -f lavfi -i "color=c=0xFF0000..."` + `color=c=0x008200`, then `ffmpeg -i chroma.mp4 -vf scdet=threshold=0.1 -f null -`. | CONFIRMED | Executed: hard red->green at matched luma scores **0.391** on scdet, red->white **60.156**. "Independent instrument" is wrong -- independent IMPLEMENTATION of a luma measurement. Recorded in 10j with the direction of the error: true recall is LOWER than every table, because the ground truth shares the blind spot. | `docs/critic/phase-a-2026-09-02.md` F6 |
| A7 | 2026-09-02 | phase-a (T3) | SCOPE | Commit `f1a385a` swept six critic scratch files (1,860 lines) into `app/gaze/bench/`, including `_critic-arch50.mjs` — a copy of `arch-arms.mjs` with the A4 staleness guard replaced by `if (false) throw`. | `git show --stat f1a385a \| grep _critic` ; `grep -n 'if (false) throw' app/gaze/bench/_critic-arch50.mjs` | CONFIRMED | Six scratch files removed from the tree and `_critic-*` added to bench/.gitignore. | `docs/critic/phase-a-2026-09-02.md` F7 |
| A8 | 2026-09-02 | phase-a (T4) | NIT | The test "IT SHIPS INERT: the default never skips a pass" reads the value from `rules/tuning.json` rather than the module default, so setting `PERSON_SKIP_EVERY = 3` in source leaves `person-skip.test.mjs` fully green (only `tuning.test.mjs` catches it). | `sed -i 's/^export var PERSON_SKIP_EVERY = 1;/export var PERSON_SKIP_EVERY = 3;/' app/gaze/src/person-skip.mjs && cd app/gaze && npx node --test test/person-skip.test.mjs` (0 fail) then `npx node --test test/tuning.test.mjs` (1 fail); `git checkout -- src/person-skip.mjs` | CONFIRMED | The inert test now asserts the MODULE default too -- read from the declaration rather than the live binding, which setPersonSkipEvery mutates. Proved red before green. | `docs/critic/phase-a-2026-09-02.md` F8 |
| B1 | 2026-09-02 | phase-b (T2/T3) | EXPOSURE | The corpus `cut` arm models a WIPE (`arch-arms.mjs:280` `tracks = []`, comment: "THE SHIPPED SCENE GATE. A cut wipes every track"). The app DEMOTES (`init-entry.js:2115` `videoTracks = demoteTracks(videoTracks)`, comment: "DEMOTE, don't wipe (review C2): boxes persist so coverage holds through the pass gap"). The wipe pays an artificial re-cover gap ONCE PER WIPE and `cutFrames` varies 100x across the swept axis (200/59/12/2), so `arch-arms.mjs:277`'s defence ("the same handicap applies to both") is false. Substituting the shipped handler flattens the exposure column to **53.5 / 55.5 / 55.0 / 55.5** across CUT_DELTA 35/60/75/90. §10k's "FALLING 67.0 -> 57.0" is **55.5 -> 55.0 (-0.5s)** under the shipped handler: 95% of the published fall is instrument. The measurement §10k calls "the direct one" cannot adjudicate F1 in either direction, leaving A1/A2's recall table (50.0% @75 vs 92.8% @60, scdet>=25) as the only evidence about the live OTA ceiling of 75. | Copy `arch-arms.mjs`, change line 280 to `tracks = (o.demote ? mod.demoteTracks(tracks) : []);`, pass `demote:true` in the `O` object of a `cut-sweep.mjs` copy, run both arms. Control must reproduce 82.5/67.0/57.0/55.5; demote reads 53.5/55.5/55.0/55.5. Second control: add the same COUNT of extra verdict frames at cuts vs at random -- at CUT_DELTA 35 only 11.5s of the 48.5s is "more verdicts", 37.0s is the post-wipe gap. | CONFIRMED | CONFIRMED and it retracts 10k in full. `arch-arms.mjs` now calls the SHIPPED `demoteTracks` from the variant module; `tracks = []` survives only under `o.cutWipe`, and `cut-sweep.mjs` takes `CUT_MODEL=wipe|demote` (default demote) and PRINTS which handler produced a table. Red-before-green both ways: `CUT_MODEL=wipe` reproduces the published row for row (35 82.5/173.5/141.0, 60 67.0/163.5/158.5, 75 57.0/157.0/162.0, 90 55.5/155.0/163.0); demote gives 53.5 / 55.5 / 55.0 / 55.5 -- FLAT and non-monotone across a 100x change in firing rate. 10k rewritten as RETRACTED IN FULL. FOLLOW-ON (10m): the demote arm was itself still biased -- it omits the app's FORCED VERDICT PASS at a cut, which is what makes a cut cheap. Modelled (`CUT_MODEL=full`, the cut frame's banked reads handed back as input) the exposure column flips a THIRD time and RISES with CUT_DELTA: 33.0 / 46.0 / 52.5 / 55.5. All three arms agree to the decimal at delta 90, where the gate fires twice -- one instrument, three handlers. | `docs/critic/phase-b-2026-09-02.md` B1 |
| B2 | 2026-09-02 | phase-b (T2) | WRONG-NUMBER | §10k "The real cost of 75 is PHANTOM (+3.5s over 60)" has the wrong SIGN. Under the shipped `demoteTracks` handler, 60 -> 75 reads phantom **182.0 -> 166.5 (-15.5s)** and false cover **170.5 -> 158.0 (-12.5s)**, with exposure flat. So 75 is better than 60 in both columns §10k names and neutral in the third: the stated reason for shipping 60 rather than 75 is contradicted in the column it cites. | Same run as B1. `DEMOTE=0` reproduces phantom 158.5 -> 162.0 (+3.5s); `DEMOTE=1` gives 182.0 -> 166.5 (-15.5s). | CONFIRMED | CONFIRMED, sign inverted. Under the shipped handler 60 -> 75 is phantom 182.0 -> 166.5 (**-15.5s**) and false cover 170.5 -> 158.0 (**-12.5s**), exposure -0.5s. The stated reason for shipping 60 is contradicted in the column it cites. CUT_DELTA is deliberately NOT moved in the same commit that admits the instrument was broken -- it travels on OTA, so the push is free and separable in a bisect. SUPERSEDED BY 10m: with the forced pass modelled, 60 -> 75 costs **+6.5s exposure** and buys 28.5s phantom, so the axis is a genuine trade with no free direction and the sign inversion B2 found was itself an artefact of the second arm. **This vindicates phase A's F1**, which I overruled twice -- and the recall proxy and the direct measurement now AGREE, which is the strongest evidence either has carried. 60 ships unchanged; the ceiling stays 75 as a BOUND, now measured to cost exposure rather than assumed free. | `docs/critic/phase-b-2026-09-02.md` B2 |
| B3 | 2026-09-02 | phase-b (T2) | DEAD-CHECK | §10i attributes `faceNoShape 48 -> 0` to "the ghost gate cannot fire on a pass that ran no model". With `PERSON_SKIP_EVERY=3` the model is not skipped, it runs ONE PASS IN THREE (`person-skip.mjs:47`; the repo's own test says "backed off, it still runs one pass in three -- never none"), and `worker-entry.js:286` disarms the gate only on the skipped passes. The stated mechanism predicts ~16, not 0. The derived cost "~0.5 extra phantom mints per second" assumes all 48 newly mint; the mechanism gives ~0.32/s. NO COUNTER CAN SETTLE IT: there is no life counter for a skipped pass anywhere in `app/gaze/src/`. | `grep -o "bumpLife('[a-zA-Z]*'" app/gaze/src/init-entry.js | sort -u` -- no skip counter. Add `bumpLife('personPassSkipped')` beside `persons.skipped = ...` and re-run `probe_skip_ab.py`: ~2/3 of passes skipped with `faceNoShape` still 0 refutes the stated mechanism. | CONFIRMED | CONFIRMED. `wantPersons` returns true on `skipsSince >= PERSON_SKIP_EVERY - 1`, so SKIP=3 runs one pass in three and the stated mechanism predicts ~16, not 0; the derived ~0.5 phantom mints/s is at most ~0.32/s on that mechanism. `bumpLife("personPassSkipped")` added on BOTH pass paths (worker and in-page) -- the counter that did not exist anywhere in `app/gaze/src`. 10i now says the mechanism is unsettled and names what would refute it: two thirds of passes skipped with `faceNoShape` still 0. | `docs/critic/phase-b-2026-09-02.md` B3 |
| B4 | 2026-09-02 | phase-b (T4) | DEAD-CHECK | The test guarding A5 is `assert.match(handler, /resetPersonSkip()/)` over `init-entry.js` SOURCE. Commenting the call out leaves `person-skip.test.mjs` **12/12 green**; only deleting the line turns it red. `docs/critic-loop.md` C2 names this shape ("a string match on source rather than a call into the code"), and the test's own comment argues against direct-call tests before substituting one. The commit's "1 fail -> 0" is true for the historical defect; the guard cannot see the ordinary ways a call is disabled. (The emitted-bundle half IS clean -- `m2()` is called inside the minified `loadstart` listener.) | `sed -i 's|^      resetPersonSkip();$|      // resetPersonSkip();|' app/gaze/src/init-entry.js && cd app/gaze && node --test test/person-skip.test.mjs` -- 12 pass / 0 fail is the defect; `git checkout -- app/gaze/src/init-entry.js`. | CONFIRMED | CONFIRMED. The handler slice is stripped of `//` and `/* */` comments before matching, so the ordinary way a call is disabled no longer satisfies it. Proved red: with `// resetPersonSkip();` the file reads **11 pass / 1 fail** where B4 measured 12/12 green; restored, **12 / 0**. (The behaviour half of the pair already called into the code and was not the defect.) | `docs/critic/phase-b-2026-09-02.md` B4 |
| B5 | 2026-09-02 | phase-b (T2) | NIT | §10k publishes a `CUT_DELTA 50` row (115 cut frames, 71.0s/167.5s/149.0s/310) that cannot be reproduced at HEAD -- there is no `bank/cuts-50.json` and `cuts.json` is stamped 60, so `cut-sweep.mjs` prints "-- no bank" for 50. The `40` row the arm DOES print (184, 82.5s/173.0s/145.5s/370) is omitted from the table, and it is the one row where exposure does not fall (82.5 -> 82.5), against "Exposure falls monotonically all the way to 90". | `cd app/gaze/bench && node cut-sweep.mjs` -- 50 prints "-- no bank", 40 prints 82.5s. `node bench/corpus-cuts.mjs 50` restores the bank if the row is to be kept. | CONFIRMED | CONFIRMED with a correction to the finding: the 50 row was REAL, its bank had been clobbered by the loop that wrote every sweep to the default path. Rebuilt with `corpus-cuts.mjs 50` and it reproduces the published row exactly -- 115 cut frames, 71.0s / 167.5s / 149.0s / 310. The 40 row is no longer omitted; the corrected 10k table publishes all six rows, and 40 is the one that shows exposure flat (53.5 = 35 row) rather than falling. | `docs/critic/phase-b-2026-09-02.md` B5 |
| B6 | 2026-09-02 | phase-b (T2) | NIT | "60 catches 92.8-95.9% of hard cuts" is recall on a population selected by scdet>=25/30, and `cut-truth.mjs`'s own output shows that selection raises the population's median OUR-delta from 48.7 (scdet>=8) to 75.0 (>=25) and 93.5 (>=30). A6 measured pearson(our delta, scdet) = 0.498, so the selection partly means "cuts our own statistic already scores high", and at >=30 the population median sits above the gate. The headline vs 45.5% is fair at the >=30 row (121 cuts over 4.2h); the figure should not travel without its row. | `cd app/gaze/bench && node cut-truth.mjs` -- read the "at a cut: p50" beside each scdet row against the CUT_DELTA column. | CONFIRMED | CONFIRMED. 10j now records that scdet and our delta are both luma statistics at pearson 0.498, so selecting on scdet >= 25 also selects toward cuts our own statistic scores high -- median our-delta 48.7 (>=8) -> 75.0 (>=25) -> 93.5 (>=30), above the gate at the top row. The percentage may not travel without its row; only the BETWEEN-COLUMN comparison at a fixed population survives. | `docs/critic/phase-b-2026-09-02.md` B6 |
| B7 | 2026-09-02 | phase-b (T2) | NIT | The whole §10i device table exists ONLY in `docs/engine-findings.md`. `probe_skip_ab.py` writes nothing to disk, and neither `712cec0` nor `8a9ed82` touches any file under `spikes/`. n=1 per arm; the arms run `for n in (1,3)` in one process with arm 1 always first, so a warm HTTP/GPU cache is an unexcluded explanation for rAF 24.1 -> 33.6 Hz. "reads/s flat at 1.00x" rests on 63 events per arm (Poisson band ~+/-25%), so "IT DOES NOT BUY VERDICTS" and "covered 61.6% vs 62.0%" are not supported at that n. (The probe DOES assert the dial took effect in both arms -- that attack fails.) | `grep -rl "3368|33.6" docs/ spikes/gauntlet/*.py spikes/gauntlet/*.txt` -> only `docs/engine-findings.md`. Persist the result dict to `spikes/gauntlet/skip-ab-<label>.json` and run once more with the arm order reversed. | CONFIRMED | CONFIRMED. `probe_skip_ab.py` now banks its full result dict to `spikes/gauntlet/skip-ab-<video>.json` and takes `REVERSED=1` to run the arms in the other order -- the confound was real and unexcluded, arm 1 always ran first. 10i carries the caveat: n=1 per arm, 63 events per arm is a Poisson band of about +/-25%, so "covered 61.6% vs 62.0%" is not a distinction this run can make and every figure in the section is a single sample until it has been run both ways. | `docs/critic/phase-b-2026-09-02.md` B7 |
| C1 | 2026-09-02 | phase-c (T3) | WRONG-NUMBER | **13a's "the cadence dial has NO trade -- exposure, false cover AND phantom all improve as the clock speeds up" is confounded with the coast window.** The fix of `5b26a04` makes the arm call `setVerdictCadence(dt * stride)`, and `person-track` derives `blurredCoastMs` from that -- so every row of the 13a table changes TWO things: verdict frequency AND coast length (k=4 4000ms -> k=1 1250ms). Hold the coast at the k=3 control's 3000ms and only exposure improves: k=3 -> k=1 is exposure 25.5 -> 9.0s, false cover 139.0 -> **141.0s (+2.0)**, phantom 465.0 -> **784.0s (+319.0, +69%)**. "Longer coast = more phantom" and "faster clock = less phantom" are different claims and 13a measures the first while asserting the second. **It also contradicts 10n in the same document** ("Verdict COUNT is what drives phantom ... this is the constraint on every cadence lever") -- the fixed-coast experiment agrees with 10n; the diagonal is what disagrees. And half the advertised span is unreachable: `tuning.mjs:118` clamps VERDICT_MAX_INTERVAL_MS to [1200, 4000], so the k=2 (told 1000) and k=1 (told 500) rows carrying the headline cannot be reached by the constant the section is about. | Copy `arch-arms.mjs`, change line 304 to `setVerdictCadence(process.env.FIXCAD ? Number(process.env.FIXCAD) : dt * stride)`, drive the uniform k family through it. Free coast reproduces 13a; `FIXCAD=1500` gives 32.0/153.0/383.0, 25.5/139.0/465.0, 13.0/122.0/499.0, 9.0/141.0/784.0 for k=4/3/2/1. Same sign on the bare arm (482.5 -> 822.0) and at `FIXCAD=2000` (551.5 -> 1020.0). | CONFIRMED | Reproduced independently: coast pinned at the control, k=3 -> k=1 gives exposure 27.5 -> 9.0s, false cover 137.0 -> 141.0s, phantom 460.5 -> **784.0s (+70%)**. The three withdrawn sentences are struck in 13a; the clamp point is recorded there too. `bench/cadence-ab.mjs` prints DIAL and CLOCK families side by side now, plus a COAST family, and `arch-arms` grew a `fixedCadence` option so the two variables can be separated at all. **It led straight to 15**: the coast is the biggest lever in the system and costs no GPU. | `docs/critic/phase-c-2026-09-02.md` C1 |
| C2 | 2026-09-02 | phase-c (T5) | WRONG-NUMBER | **The restated headline in SHIPPED SOURCE swapped instruments.** `cadence.mjs` now says "man exposure is 24.5s at 1.5s per verdict and 5.5s at 0.5s", replacing 81.0s/8.0s. 2 names the instrument the old pair came from -- `bench/cadence-ab.mjs`, whose arm is `{hold, clampPad, cut, inertNoSignal, memSignal, mem: loose2/loose}`. The new pair is from `cadence-place.mjs` (new in `5b26a04`), whose arm is `{hold, clampPad, cut}` -- three shipped behaviours short, where `bar-ab`, `cut-value`, `down-ab`, `fc-attrib`, `fc-why`, `grace-ab`, `instant-ab`, `closeup-ab` and `cadence-ab` all pass the full set. **Same-instrument, the restatement is 27.5s against 11.5s** -- the 0.5s figure is 2.1x the published one -- and every false-cover figure in 13a/13b is 35-49s off (186.0 vs 137.0 at k=3). **And the old figure is still published unretracted in four files**: `engine-findings.md:102-110` (2, the canonical table, plus an untouched "his band only" block), `engine-findings.md:677` (10i), `plan-engine-v3.md:65,74,76,201,790,880`, `research/temporal-2026-09-02.md:52,237,356,358,391`. | `cd app/gaze && node bench/cadence-ab.mjs` -> 27.5/137.0/460.5, 18.5/109.5/359.0, 11.5/102.5/343.5. Copy `cadence-place.mjs` with the full option set on line 96 and it reproduces those three rows exactly, identifying the options as the cause. `grep -rn "81[.]0" docs/` for the eleven unretracted lines. | CONFIRMED | The 81.0/8.0 pair is retired everywhere: `plan-engine-v3.md` and `research/temporal-2026-09-02.md` carry a retraction banner naming all three instrument defects, and engine-findings' two inline quotations are marked superseded in place. His regime is **22.0 / 155.0 / 573.5** with C4+C6 corrected -- phantom understated by a factor of four against the old 144.0s, and that table's phantom column is INVERTED, not merely wrong. | `docs/critic/phase-c-2026-09-02.md` C2 |
| C3 | 2026-09-02 | phase-c (T4) | WRONG-NUMBER | **13b's PLACED benefit is the coast the median-gap inference handed it, not the placement.** The stride is the MEDIAN gap, and in a starvation-dominated policy most gaps ARE `MAXGAP` -- so the placed arms are told a cadence per window that differs from the control's at the identical MEAN gap of 3.00 frames: at max gap 4, **13 of 18 windows get told 2000ms / coast 4000ms** against the uniform control's 3000ms; at max gap 6, 9 of 18 get **coast 6000ms**. Hold the coast equal across all arms and the published "buys 8.5s of exposure and 16.0s of phantom for 31.5s of false cover" becomes **exposure +0.0s, phantom -2.0s (a LOSS), false cover -30.5s**. Both advertised gains vanish and one inverts. The section's "the comparison is placement against placement and never budget against budget" holds the budget but not the quantity 13 had just established is decisive; "the max gap is the load-bearing half" is measured on an arm where the max gap also sets the coast. | `FIXCAD=1500 node <copy of cadence-place.mjs importing the FIXCAD copy of arch-arms> 4,6` -> UNIFORM k=3 27.5/137.0/460.5, PLACED gap 4 **27.5/167.5/462.5**, PLACED gap 6 36.0/177.5/452.5. Free coast, same file: 18.0/166.0/438.0 and 50.0/186.5/398.0. Strides per window from `placedSet` confirm 13 of 18 at stride 4. | CONFIRMED | `cadence-place.mjs` tells every arm `HIS_EFFZOOM`. The conclusion flips: placement buys 7.0s of exposure and COSTS 32.5s of false cover and 80.5s of phantom, where the published version claimed it bought exposure AND phantom. The phantom benefit was the coast. 13b rewritten, including its "worth a proper round because it is the only cadence lever that does not spend more GPU" argument, which the coast dial now beats on its own terms. | `docs/critic/phase-c-2026-09-02.md` C3 |
| C4 | 2026-09-02 | phase-c (T1) | WRONG-NUMBER | **"THE CADENCE THE TRACKER IS TOLD MUST BE THE CADENCE IT GETS" is not what the app does.** `init-entry.js:4036` calls `setVerdictCadence(effZoom)` and `effZoom` (`:3392`) is `min(VERDICT_MAX_INTERVAL_MS, max(ZOOM_INTERVAL_MS, lastVerdictMs * 4))` -- the SCHEDULED cap, not the achieved gap. `cadence.mjs`'s own measured table says his Redmi is **cap-limited in both arms, effZoom = 2000**, while 10n records the achieved gap in the same window as **1.55s**. The repo holds both numbers and knows they differ; the fix fed the arm the gap where the app feeds the cap, replacing one mismatch (told 500 / gets 1500) with another (told 1500 / app tells 2000). Modelled at his device's effZoom, "his regime" -- the row every corpus decision is quoted in -- reads **23.5s / 152.0s / 568.0s** against the published 27.5/137.0/460.5: **phantom understated by 107.5s (+23%)**, the column 10n names as the constraint on every cadence lever. With C6 as well the row is 22.0/155.0/573.5. | `FIXCAD=2000 KS=3 node <the FIXCAD copy>` -> 23.5/152.0/568.0 against `KS=3` -> 27.5/137.0/460.5. Cross-check the premise in `app/gaze/src/cadence.mjs` ("BOTH ARMS ARE CAP-LIMITED ... effZoom min(2000, 5000) = 2000") and `engine-findings.md` 10n ("58 verdicts, i.e. one every **1.55s**"). | CONFIRMED | Verified in the shipped source (`init-entry.js:4036` hands the cap-pinned `effZoom`) and reproduced to the decimal: 23.5 / 152.0 / 568.0. `HIS_EFFZOOM = 2000` is named in `arch-arms` with the citation so the next arm claiming "his regime" has a number instead of a stride to guess from. **It also invalidated a recommendation made an hour earlier**: the coast dial re-swept at his real told names 1.33, and the 1.67 committed before it is a no-op there. | `docs/critic/phase-c-2026-09-02.md` C4 |
| C5 | 2026-09-02 | phase-c (T1) | DEAD-CHECK | **The stated invariant is false and one bench still runs the defect.** `arch-arms.mjs:286-291` says "`thin` marks every frame it silenced by moving the reads to `_labelFaces`, so the stride is a property of the window the arm was handed and cannot disagree with it." There is no shared `thin` -- every bench inlines its own, and `bench/matrix.mjs:76-83` has a second mode that marks `_noRead` on the faces instead: `return { ...fr, faces: fr.faces.map((f) => ({ ...f, _noRead: true })) }`. A `_noRead` face is a positionOnly observation in `obsOf`, i.e. a non-verdict frame the inference cannot see, so `matrix.mjs`'s `[3, 'position']` arm (printed on every run, labelled "his cadence, general regime") is handed **stride 1** and still tells the tracker 500ms against a 1500ms gap -- the exact defect 13 exists to fix. **And nothing can catch it**: `git show --stat 5b26a04` touches no test and `grep -rln arch-arms test/` is empty, so the change that "rebases every corpus number in this repo" ships with no check that can go red. | Scratch script applying both thinning styles to one window and running the inference: coast-style gives stride 1/2/3/4 at k=1/2/3/4; position-style gives **1 at every k**. `grep -n "_noRead" app/gaze/bench/matrix.mjs app/gaze/bench/arch-arms.mjs`. | CONFIRMED | `isVerdictFrame` now recognises both thinning styles, and `inferCadence` is EXPORTED with `test/cadence-infer.test.mjs` covering both modes at k=1..4, the median-gap property, an empty-but-read frame, and the no-verdicts fallback that would otherwise hand NaN to a function guarding `ms > 0`. Red-before-green: reverting the predicate fails exactly the three position-mode rows. | `docs/critic/phase-c-2026-09-02.md` C5 |
| C6 | 2026-09-02 | phase-c (T1/T6) | WRONG-NUMBER | **The sibling number was left at the bank interval.** `person-track.mjs:1075` -- "credit moves by the gap between READS (`obs.verdictDt`), not the pass interval" -- and `clearMs += vdt` at `:1233`. The app computes it as `Math.min(1000, now - lastZoomAt)` (`init-entry.js:3601`); the arm passes `dt` = the BANK interval, 500ms, in every arm at every k, exactly as `setVerdictCadence` did before `5b26a04`. At k=3 the app passes 1000 and the arm passes 500, so a man needs three verdicts to reach the clear hold where the device needs two. Effect at k=3: **-2.0s exposure, +2.0s false cover, +4.5s phantom** -- the size of every constant this repo swept in August (2: "every constant swept in August moves 1-3s"), and it compounds with C4 rather than cancelling. | Add `const VDT = Math.min(1000, dt * stride)` beside the stride block in a copy of `arch-arms.mjs` and pass it at the single `obsOf(f, mm, dt, ...)` call (line 581). `KS=3` -> 27.5/137.0/460.5; `VDT=1 KS=3` -> 25.5/139.0/465.0; `VDT=1 FIXCAD=2000 KS=3` -> 22.0/155.0/573.5. | CONFIRMED | The arm derives `vdt = min(1000, dt * stride)` from ARRIVAL, as the app does, and passes it at both observation sites. With C4 the shipped row lands on **22.0 / 155.0 / 573.5** -- exactly the value the critic predicted before it was run, which is the closest thing to a cross-check this instrument has had. | `docs/critic/phase-c-2026-09-02.md` C6 |
| C7 | 2026-09-02 | phase-c (T6) | NIT | **`5b26a04` updated the birth-rung figure in shipped source and not in the findings doc, leaving them contradicting each other.** `person-track.mjs:189` now says "-19.5s of false cover ... FOR +1.0s OF EXPOSURE"; `docs/engine-findings.md:1061` still publishes "shipped cut handler \| +5.0s \| **-25.5s** \| -6.0s" in the very table whose point is that this figure keeps moving. The phantom column moved too (-12.0s, not -6.0s). The commit body claims "Rebased everything that had travelled". | `cd app/gaze && node bench/birth-ab.mjs` -> man k=3 delta **+1.0s / -19.5s / -12.0s**, matching `person-track.mjs` and not `engine-findings.md:1061`. | CONFIRMED | 10m carries the third row (+1.0s / -19.5s) and records all three superseded values. A figure that has moved three times in one direction for three different instrument reasons is worth less than the fact that it keeps moving, and the table says so. | `docs/critic/phase-c-2026-09-02.md` C7 |
| C8 | 2026-09-02 | phase-c (T4) | NIT | Two small ones in the new `cadence-place.mjs` / `corpus-cuts.mjs` pair. (1) **`MINGAP` is inert.** 13b describes the policy as three parts and names MINGAP "the app's own ZOOM_INTERVAL_MS floor (400ms, ~1 banked frame)", but in `placedSet` `i` always exceeds `last`, so `i - last >= 1` is a tautology and the knob can never bind. (2) **`bank/deltas.json` has no freshness assert.** `cadence-place.mjs` throws if the file is absent or missing a tag but never checks its `__meta` (`GATE_SIZE 16, rate 10`) against `scene-gate.GATE_SIZE` -- the class A4 was filed for on `cuts.json`, whose fix is `assertCutsFresh`. The new bank shipped without an equivalent. | Recompute `placedSet` with MINGAP 1 / 0 / -99: verdict counts are **identical** (721/721/721 at max gap 4 T=13.2; 720/720/720 at max gap 6 T=7.1). For (2), `grep -n "__meta" app/gaze/bench/cadence-place.mjs` returns nothing. | CONFIRMED | (1) `MINGAP` is documented as inert at the corpus's 2fps rather than left for a reader to infer the floor was exercised. (2) `deltas.json` is checked against the mtimes of the windows it was derived from -- a stale bank makes the placement policy choose frames from footage that has since been re-banked. | `docs/critic/phase-c-2026-09-02.md` C8 |
| D1 | 2026-09-02 | phase-d (T4) | EXPOSURE | **The `PTRACK_MIN_COAST_PASSES` clamp floor of 1.33 is non-protective at every cadence at or below ~1503ms, and `VERDICT_MAX_INTERVAL_MS` on the SAME OTA channel reaches 1200.** `tuning.mjs` and `setCoastPasses`'s docstring both justify the floor as "1.0 really does reach 2000ms, costing +16.0s (man) and +10.0s (woman) of exposure ... THAT is what the floor refuses". The coast is `min(max(2000, passes*told), max(900, 2.5*told))`, so the `passes` term only reaches the answer while `passes*told > 2000` -- at 1.33 that needs `told > 1504`. At told 1500 the OTA floor 1.33 and the raw 1.0 produce the SAME 2000ms coast and the SAME corpus row. A single `rules/tuning.json` carrying `{VERDICT_MAX_INTERVAL_MS: 1200, PTRACK_MIN_COAST_PASSES: 1.33}` has both values inside both clamps and lands the coast at 2000ms -- and 13a already recommends that cadence move. Any device whose verdict costs 250-375ms lands there with nothing pushed at all. The protected quantity is the coast in MILLISECONDS and the clamp is expressed in PASSES. | `cd app/gaze && GENDER=man K=3 TOLD=1500 node bench/coast-ab.mjs 1.0,1.33,1.5,2` -> passes 1.0 and 1.33 both print `2000ms 38.0s 132.5s 346.0s 212` against SHIPPED `3000ms 25.5s 139.0s 465.0s 174`: **+12.5s of exposure from a value the clamp calls safe**. `GENDER=woman` same command: 1.0 and 1.33 both `36.5 / 185.0 / 399.0` against shipped `30.0 / 194.5 / 545.5`, +6.5s. | CONFIRMED |CONFIRMED and reframed. A uniform ms guarantee cannot exist through a passes-clamp (holding 2660ms at told 1200 needs 2.22, above the shipped 2). What holds at every cadence is PTRACK_MAX_COAST_MS: no push takes the coast below 2000ms, and that is what the test asserts now. The cadence-dependence and the joint VERDICT_MAX_INTERVAL_MS push are written into tuning.mjs and engine-findings 15c. Report carries the DERIVED coast (tuning.coastMs/toldMs).| `docs/critic/phase-d-2026-09-02.md` D1 |
| D2 | 2026-09-02 | phase-d (T1/T2) | WRONG-NUMBER | **14 and 14a are published as "same corpus, same k=3, man mode" and run UNTHINNED.** `arch-ab.mjs` and `critic-lowbar.mjs` contain no `thin()`, so `inferCadence` returns stride 1 on all 18 windows, the tracker is told 500ms, and it coasts `min(max(2000,1000), max(900,1250))` = **1250ms** -- the exact row 13's own table names as the broken one, in the two tables published beside it. Re-run at k=3 / told 2000 with the option set `coast-ab.mjs` calls shipped, phantom is understated ~2x throughout and one published sentence inverts: "raising the bar to 0.80 ... more than doubles false cover -- it is covering everyone" is 430.0s at k=1 and **218.5s** at k=3. Second mislabel in the same table: the row printed "A0 shipped" is `ARM({})`, which `arch-arms.mjs` itself labels **1078**, while `ARM_A0_HOLD` is labelled "1079, what ships" -- no hold, no cut, no adjacency clamp, no identity memory. | `cd app/gaze && GENDER=man node bench/arch-ab.mjs` reproduces the published 14 table exactly (7.5/188.5/282.5 ... 1.5/430.0/408.5). `inferCadence` on every unthinned window returns `stride 1`. Re-run with `thin(win,3)` + `fixedCadence: HIS_EFFZOOM` and the same five arms read 22.0/155.0/573.5, 23.5/212.0/628.0, 20.5/212.5/632.0, 19.5/214.0/636.0, 19.5/218.5/644.5. | CONFIRMED |CONFIRMED. arch-ab and critic-lowbar thin at K_HIS and take hisRegimeOpts. Tables re-derived in 14c, both gender arms; conclusion stronger, the 0.80 sentence withdrawn, baseline is ARM(hisRegimeOpts) not ARM({}).| `docs/critic/phase-d-2026-09-02.md` D2 |
| D3 | 2026-09-02 | phase-d (T2) | WRONG-NUMBER | **14a swept two constants together, one of which is provably inert in the arm it ran, and its conclusion is false in the woman arm.** The bar is chosen by the READ's label, so man mode reads only `GENDER_CLEAR_SCORE` and woman mode only `GENDER_CLEAR_SCORE_FEMALE` -- the man-mode table therefore never tested `_FEMALE`, which is a separate OTA key with its own clamp [0.30, 0.90] and has never been swept alone in any regime. And 14a's unqualified "every step down costs exposure and buys almost nothing ... should not be pushed lower over the OTA channel" is a man-mode statement: in the woman arm, his regime, 0.45/0.35 -> 0.40/0.30 is exposure **identical** (25.5), false cover -2.0s and phantom -13.5s -- better or equal on all three columns. Same defect class as 14's own headline, one subsection later. | `cd app/gaze`, k=3 / told 2000 / shipped opts: man `0.45/0.35` and `0.45/0.25` both print `22.0 / 155.0 / 573.5`; woman `0.45/0.35` and `0.25/0.35` both print `25.5 / 201.0 / 679.5`. Woman `0.45/0.30` prints `25.5 / 199.0 / 666.0`. | CONFIRMED |CONFIRMED. Each constant swept ALONE per gender arm: seven identical rows show each mode reads exactly one. Recommendation restated with the mode named in 14c -- GENDER_CLEAR_SCORE do not push; _FEMALE 0.35->0.30 is free on this corpus and inert in his mode, so recorded not pushed.| `docs/critic/phase-d-2026-09-02.md` D3 |
| D4 | 2026-09-02 | phase-d (T1) | SCOPE | **14's justification for deleting `nmWeight` and `ghost` -- "read NOWHERE ... neither has a specification anywhere in the repo" and "gone from the call sites" -- is false on both halves.** (a) Both were SPECIFIED AND IMPLEMENTED in `6f0b7ff` (`opts.nmWeight  drop reads below the nm floor from the pool`; `opts.ghost  a subject whose reads are ALL signal-free stops being covered`, with `GHOST_VOTES`), and that commit's own table shows the ghost arm producing DIFFERENT rows (A3 4.0/119.5/107.5 vs A5 1.5/97.0/104.5). `5974e9d` deleted the behaviour and left the labels -- so an arm this repo once had, and once measured as material, is silently gone. (b) Four bench files still pass `nmWeight: true`, untouched by this commit, including `critic-poolbar.mjs`. (c) The nm floor is now UNCONDITIONAL (`if (f.nm >= NM_FLOOR)`, no guard), so `nmWeight:true` is hard-wired on and the published rung "A1 per-subject window + hold" (a pool WITHOUT the nm floor) has no implementation at all -- A2 - A1 was zero for a second, independent reason. | `git show 6f0b7ff -- app/gaze/bench/arch-arms.mjs \| grep -n 'opts.nmWeight\|opts.ghost\|GHOST_VOTES'` returns the spec comments and the implementations. `git log --oneline -S"opts.ghost" -- app/gaze/bench/arch-arms.mjs` -> `5974e9d`, `6f0b7ff`. `grep -rn nmWeight app/gaze/bench/*.mjs` -> critic-mixedhide:17, critic-neighbour:14, critic-poolbar:18, perf-cadence:50. | CONFIRMED |CONFIRMED. Names deleted from all four remaining call sites. arch-arms records that both were specified and implemented in 6f0b7ff, deleted by 5974e9d, that the nm floor is now UNCONDITIONAL, and that the published A1 rung has no implementation.| `docs/critic/phase-d-2026-09-02.md` D4 |
| D5 | 2026-09-02 | phase-d (T6) | WRONG-NUMBER | **13b's "UNIFORM k=3 (today)" and 15a's "his regime, SHIPPED" are different arms, quoted in one document as the same thing.** `cadence-place.mjs:132` builds `{hold, clampPad, cut, fixedCadence}`; `cadence-ab.mjs:63` and `coast-ab.mjs` add `inertNoSignal`, `memSignal` and `mem` -- the identity-memory behaviour shipped in 1084. Same corpus, same k=3, same told=2000, the two option sets read **20.5 / 197.0 / 592.0** and **22.0 / 155.0 / 573.5**: 42.0s (27%) of false cover apart. 13b's closing sentence subtracts across them ("7.0s of exposure at a cost of 80.5s of phantom, when the coast dial (15a) buys 149.5s") without saying the baselines differ. It also means the placement arm is scored without the identity memory the app ships, and re-birth suppression is the mechanism placement acts on. | Two `makeArms(await import('./.cache/shipped.mjs'))` calls over `thin(win,3)`, `GENDER=man`: `{hold,clampPad,cut,fixedCadence:2000}` -> 20.5 / 197.0 / 592.0; the same plus `{inertNoSignal, memSignal, mem:'loose2'}` -> 22.0 / 155.0 / 573.5. | CONFIRMED |CONFIRMED. hisRegimeOpts is the single definition of the shipped baseline; cadence-place, coast-ab, cadence-ab, arch-ab and critic-lowbar all call it. 13b re-derived in 15b -- placement's cost falls to 20.5s fc / 67.0s phantom and the verdict is unchanged.| `docs/critic/phase-d-2026-09-02.md` D5 |
| D6 | 2026-09-02 | phase-d (T4) | NIT | **`setCoastPasses`'s docstring and `tuning.mjs` disagree about the cap, in the same commit.** person-track: "`cap = max(PTRACK_MAX_COAST_MS, passes * ms)` **binds at every value his device reaches** -- the 2.5x term is 5000 at his cadence and **never wins** -- so this one number IS the coast." tuning.mjs, one file away: "The ceiling is 3.0 because past ~2.5 the 2.5x term wins and the dial stops doing anything." The second is right: the top third of the clamp range is a no-op. | `cd app/gaze && GENDER=man K=3 node bench/coast-ab.mjs 2,2.5,3` -> 2.5 and 3 both print `5000ms 21.0s 165.0s 624.5s 160`, identical rows. | CONFIRMED |CONFIRMED. person-track's docstring rewritten to say which term binds at which cadence. New test pins that 2.5 and 3.0 give the identical coast.| `docs/critic/phase-d-2026-09-02.md` D6 |
| D7 | 2026-09-02 | phase-d (T2) | NIT | **`_patch.mjs` states a byte-identity contract that its own named example violates, and nothing tests it.** "A value equal to the shipped one is allowed on purpose: that is the control point of a sweep and it must produce a byte-identical bundle." True for the two constants swept tonight; FALSE for `PTRACK_MAX_COAST_MS`, the constant cited six lines above in the same file ("esbuild writes 2000 as 2e3"). Semantically harmless (`Number('2e3') === 2000`) but the contract is false for every exponent-form or leading-dot literal and no test asserts it. | `patchConsts(src, {GENDER_CLEAR_SCORE:0.45, GENDER_CLEAR_SCORE_FEMALE:0.35}) === src` -> **true**; `patchConsts(src, {PTRACK_MIN_COAST_PASSES:2}) === src` -> **true**; `patchConsts(src, {PTRACK_MAX_COAST_MS:2000}) === src` -> **false**, first diff `var PTRACK_MAX_COAST_MS = 2e3;` -> `= 2000;`. | CONFIRMED |CONFIRMED. Contract restated as NUMBER-identical, with the 2e3 counter-example named. controlIsByteIdentical exported; test/patch-control.test.mjs pins all three halves and was proved red.| `docs/critic/phase-d-2026-09-02.md` D7 |
| D8 | 2026-09-02 | phase-d (T3) | NIT | **`coast-ab.mjs`'s header computes in the 1500ms regime 15a retracted, while the file itself now defaults to `TOLD = HIS_EFFZOOM = 2000`.** Its worked example is `blur = min(cap, max(900, 3750))` (2.5 x 1500), and it asserts "floors it at 2000ms whatever is pushed -- the OTA channel cannot drive the coast below what the code already treats as the minimum cap ... That floor is a protection property". That sentence is true of `PTRACK_MAX_COAST_MS` and is exactly why D1's clamp floor is redundant below told 1504; left as written it reads as support for a floor that does not bind there. | Read `app/gaze/bench/coast-ab.mjs:12-24` against `:52` (`const TOLD = Number(process.env.TOLD \|\| HIS_EFFZOOM)`) and against `arch-arms.mjs`'s `export const HIS_EFFZOOM = 2000`. | CONFIRMED |CONFIRMED. Header re-worked at told 2000 and now says explicitly that the protection property is PTRACK_MAX_COAST_MS and NOT the clamp floor.| `docs/critic/phase-d-2026-09-02.md` D8 |
| D9 | 2026-09-02 | phase-d (T4) | NIT | **`blurredCoastBudgetMs` was exported because "a dial nobody can read is a dial nobody can verify pushed", and the coast is still unreadable on the device.** Its only consumer is `test/tuning.test.mjs`; it reaches no report. `ids.tuning.applied` (init-entry.js:144) carries the pushed PASSES value, not the derived coast -- and D1 is precisely the case where one pushed value means two different coasts depending on the device's effZoom. The next round cannot confirm a push landed. | `grep -rn blurredCoastBudgetMs app/gaze/src app/gaze/test` -> `src/person-track.mjs` (definition) and `test/tuning.test.mjs` only; `grep -n coast app/gaze/src/diag-report.mjs` -> nothing. | CONFIRMED |CONFIRMED. init-entry stamps tuning.coastMs and tuning.toldMs on every verdict pass; diag-report passes both. Verified in the EMITTED bundle: we.tuning.coastMs=IX(),we.tuning.toldMs=ze.| `docs/critic/phase-d-2026-09-02.md` D9 |
| D10 | 2026-09-02 | phase-d (T6) | NIT | **The new clamp test's justification quotes numbers 15a retracted in the same commit.** `tuning.test.mjs` says "in his regime 23.5s at the shipped value against 40.5s at passes 1.0" and its assertion message says "+17.0s of exposure"; 15a's table and `tuning.mjs` both say 22.0 and 38.0 = **+16.0s**. 23.5 / 40.5 are the pre-C6 numbers (`verdictDt` = the bank interval), superseded by the same diff that wrote the test. | `grep -n '23.5\|40.5\|17.0' app/gaze/test/tuning.test.mjs` against `docs/engine-findings.md` 15a's table (`1.0 -> 38.0`, `2 SHIPPED -> 22.0`) and `app/gaze/src/tuning.mjs` ("+16.0s (man)"). | CONFIRMED |CONFIRMED. Test rewritten; the retracted 23.5/40.5 pair is gone.| `docs/critic/phase-d-2026-09-02.md` D10 |
| D11 | 2026-09-02 | phase-d (T4) | DEAD-CHECK | **`test('the coast dial cannot be pushed below the shipped cap floor')` exercises the one cadence at which the guard works.** It calls `setVerdictCadence(2000)` and asserts `blurredCoastBudgetMs() === 2660` with the message "it refuses the 2000ms window that costs +17.0s of exposure". At `setVerdictCadence(1500)` the same clamped floor yields **2000** -- the window it says it refuses (D1). As written it can only fail if someone changes the arithmetic at exactly told 2000; it cannot fail for the property its own name claims. | `setCoastPasses(2); setVerdictCadence(told); applyTuning({PTRACK_MIN_COAST_PASSES: 1.0})` then read `blurredCoastBudgetMs()`: told 1200 -> 2000, 1500 -> 2000, 2000 -> 2660. The raw (clamp-bypassed) `setCoastPasses(1.0)` gives 2000 at all three. Fix: loop the test over told [1200,1500,2000,3000]; it goes red until D1 is fixed. | CONFIRMED |CONFIRMED. The test loops told over [1200,1500,1600,2000,3000], asserts the coast never falls below PTRACK_MAX_COAST_MS, and asserts explicitly that below 1504 the floor equals raw 1.0. Proved red two ways (floor 1.33->1.0; cap ignoring PTRACK_MAX_COAST_MS).| `docs/critic/phase-d-2026-09-02.md` D11 |
| D12 | 2026-09-02 | phase-d (T5) | NIT | **15a's "the closest thing to a cross-check this instrument has had" is a replication, and its instrument no longer exists.** The phase-C 22.0/155.0/573.5 came from `bench/_critic-cadsep-full.mjs` (`FIXCAD=2000 VDT=1`), which is not in git and not on disk -- the "cross-check" cannot be re-run. And it was never independent: both computations use the same corpus, the same `corpus-score.mjs`, the same `arch-arms` `ARM`, the same 500ms bank grid and the same option set; only the two corrections differ. It confirms the implementation matches the patch and shares every assumption underneath it. The real second population in 15a is the WOMAN arm, which does agree on direction and winner. | `git log --all --oneline -- app/gaze/bench/_critic-cadsep-full.mjs` -> empty; `ls app/gaze/bench/_critic*` -> absent. The tonight-side row is reproducible from `makeArms(shipped)({hold,clampPad,cut,inertNoSignal,memSignal,mem:'loose2',fixedCadence:2000})` over `thin(win,3)` -> 22.0 / 155.0 / 573.5. | CONFIRMED |CONFIRMED. 15d restates it as a replication and names the two real cross-checks. The leave-one-out qualifications (31% of phantom in one window; fc improves in 5/18; exposure is 1-3 frames across five windows) are recorded there too.| `docs/critic/phase-d-2026-09-02.md` D12 |
| E1 | 2026-09-02 | phase-e (T1/T3) | DEAD-CHECK | `cadence-pinned.test.mjs` never calls `makeArms`, so the guard that stops an arm running at the derived 1250ms coast is tested only against `warnDerivedCadence` in isolation. `makeArms` could stop calling it and the suite would stay green -- which is the exact shape of the D2 defect the guard exists to prevent. | Delete the `warnDerivedCadence` call inside `makeArms` and run `node --test test/`. Green. | CONFIRMED | Two tests added that build an arm THROUGH `makeArms` with hand-rolled options and assert the warning reaches stderr, plus a companion asserting `hisRegimeOpts` is silent so the guard is not simply always on. Red-proven by removing the call. | `docs/critic/phase-e.md` |
| E2 | 2026-09-02 | phase-e (T1/T3) | DEAD-CHECK | The 1089 anti-squash guard in `crop-geometry.test.mjs` matched ONE spelling of the four-argument `drawImage`. Rewriting the squash as `drawImage(video, 0, 0, canvas.width, canvas.height)` leaves fifteen tests green with the frame squashing again -- on the only path Reddit, X, Instagram and Facebook have. | Replace the fitted draw in `init-entry.js` with `ctx2d.drawImage(video, 0, 0, ctx2d.canvas.width, ctx2d.canvas.height)` and run the suite. Green. | CONFIRMED | The test now PARSES every `drawImage` in the file with balanced-paren argument splitting and asserts the complete set of five-argument whole-frame draws equals the two documented deliberate squashes (the luma gate and MoveNet). Any third one is red whatever it is spelled as. Red-proven with the critic's own rewrite. | `docs/critic/phase-e.md` |
| E3 | 2026-09-02 | phase-e (T1/T3) | EXPOSURE | 16a claims the 1089 letterbox moved the whole-frame boolean 'once, in the harmless direction'. At N=241 the letterbox produces 3 frames with zero detections where the squash produced some, and 79% of the lost detections are under 64px. On the four non-YouTube platforms `wholeFrameFlagged` is the ENTIRE pipeline, so a blind frame is `cleanStreak++` and four in a row reach `clearEl`. | Re-run the 16a arms and count frames where `faces.length === 0` per arm rather than counting the boolean. | CONFIRMED | Three counters added -- `wholeFrameSamples`, `wholeFrameNoFaces`, `wholeFrameCleared` -- bumped on BOTH the worker and in-page detector paths and at the reveal itself, seeded to 0 so absent cannot be mistaken for never-hooked. 16a's HONEST LIMITS rewritten: the one-way claim is withdrawn and the blind-frame rate is now a number any artifact off those four platforms carries. `test/whole-frame-counter.test.mjs`, 5 tests, each red-proven; verified in the EMITTED bundle. | `docs/critic/phase-e.md` |
| E4 | 2026-09-02 | phase-e (T1/T3) | DEAD-CHECK | `iou-where.mjs` -- the per-window instrument that justified shipping 1090 -- defaults its candidate to the literal 0.15. 1090 shipped 0.15, so at HEAD it compares the shipped value against itself and prints `0 of 18 windows moved` in both arms. The tool that justified the release now reports the release changed nothing. | Run `node bench/iou-where.mjs` at HEAD. | CONFIRMED | The default is derived from the shipped constant, and a candidate EQUAL to it exits 2 with a message rather than printing a false null. | `docs/critic/phase-e.md` |
| E5 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | The association dial was only ever swept BELOW the shipped value. `iou-ab.mjs` stops at the shipped number and walks down, which was right while 0.20 shipped and hides the tightening direction now 0.15 does. And the birth decomposition E5 rests on (310 births, fresh 74.2%, contended 10.3%) came from `births.mjs`, the FOURTH file in the D2 class. | Add rows above the shipped value to the ladder; re-run `births.mjs` with `hisRegimeOpts`. | CONFIRMED | The ladder now walks BOTH ways. `births.mjs` regime fixed and it REVERSES: 147 births, `birthFresh` 26.5% is the SMALLEST class and `birthContended` 44.2%/51.0% the LARGEST -- an assignment problem, not the threshold one E5 pointed at. Decomposed (told 500 -> 2000 alone moves births 214 -> 147) and cross-checked against 10o's independent sweep, which reports the same 147 at IOU 0.15 in both genders. Acted on: `src/assign.mjs` (Hungarian, cardinality-first) ships in 1091. Findings 17/17a/17b. Residual open as E5b. | `docs/critic/phase-e.md` |
| E5b | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | The re-run ladder still stops at 0.30. The OTA clamp ceiling is 0.35 and that row has never been measured, so the ceiling is a bound nobody has priced. | Add 0.35 to `LADDER` in `iou-ab.mjs` and run both arms. | CONFIRMED | Measured. At the ceiling, against the shipped 0.15: **man -6.5s exposure for +32.0s false cover and +59.0s phantom; woman -4.5s for +8.5s and +97.0s**, both arms agreeing on the sign of all three. The mechanism is in the birth counts -- tightening raises births 141 -> 184 (man) and 136 -> 184 (woman), because a subject that fails to re-associate is re-minted BLURRED. So the whole clamp range is one monotone protection trade and the ceiling is a SHIPPABLE STATE, not a guard rail. 0.35 added to `LADDER`; findings 17b; raw `spikes/gauntlet/iou-ladder-ceiling.txt`. | `docs/critic/phase-e.md` |
| E6 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | Three different 'shipped baseline' control triples are in circulation across the docs and the benches, and the published self-check does not reproduce at HEAD. A self-check that cannot detect its own staleness is worse than none: it certifies a bench as in-regime when it is not. | Run the shipped arm over the corpus and compare against every triple quoted in the tree. | CONFIRMED | `arch-arms.CONTROL` is the single source of truth now (config string, `since` version, both arms), and `test/control-triple.test.mjs` RUNS the shipped arm over the corpus in 370ms and asserts it -- so a constant that moves without the triple moving goes red. Red-proven by flipping `PTRACK_ASSIGN` to greedy. 1091: man 22.5/136.5/547.5, woman 25.5/201.5/628.0. | `docs/critic/phase-e.md` |
| E7 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | 10p's `CUT_DELTA` table was measured at `PTRACK_IOU_MIN` 0.20 and every row moved when 1090 shipped 0.15. The quoted 14.0s of false cover for 60 -> 75 is 6.0s at HEAD. | Re-run `cut-sweep.mjs` in his regime at the shipped IOU. | CONFIRMED | SUPERSEDED banner on 10p naming the regime it was measured in, and 17c carries the re-priced table under the 1091 assignment (60 -> 75 is man +5.0/-5.0/-93.0, woman +1.5/-3.0/-37.0). The refusal is unchanged and rests on HIS luma p95 of 54.9, not on the corpus. | `docs/critic/phase-e.md` |
| E8 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | The coast table in SHIPPED SOURCE (`person-track.mjs`), the only place the owner's one open decision is priced, was still on the superseded baseline at HEAD. He is being asked to rule on digits his phone would not produce. | Read the table against a fresh sweep at the shipped constants. | CONFIRMED | Regenerated in place at the 1091 baseline. 2 -> 1.33 is now +5.0s man / +4.0s woman of exposure for 141.0s and 156.5s of phantom (-25.7%, -24.9%), against greedy's +4.5/+4.0 for -149.5/-185.0. Same shape, same winner, same trade: the standing question stands as asked and only its digits move. Findings 17c. | `docs/critic/phase-e.md` |
| E9 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | 10o's conclusion contradicts the binary that shipped, in all three clauses -- it describes `PTRACK_IOU_MIN` as not on the OTA channel and 0.15 as unreachable without an install, when 1090 shipped 0.15 AND published the key. | Read `tuning.mjs`'s whitelist and the 1090 APK's emitted bundle. | CONFIRMED | The tail of 10o is replaced with a full retraction rather than an edit: the three clauses are struck and the shipped state is stated. | `docs/critic/phase-e.md` |
| E10 | 2026-09-02 | phase-e (T1/T3) | SCOPE | The range appends a SECOND `10g` and a SECOND `10h`, colliding with existing sections -- one of which the ledger records as RETRACTED. A citation to `10g` is now ambiguous between a live finding and a withdrawn one. | Count `## 10g` headings in `docs/engine-findings.md`. | CONFIRMED | Renamed to `10o` and `10p`; every citation in `arch-arms.mjs`, `person-track.mjs` and the docs updated. 0 duplicate ids remain in the file. | `docs/critic/phase-e.md` |
| E11 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | The constitution now says both '75 is the next step, one OTA push' and '75 is REFUSED'. Two directives on one constant, and the reader cannot tell which is current. | Read both sentences in `docs/engine-findings.md`. | CONFIRMED | The earlier sentence is annotated in place with the refusal and its reason (at 75 the gate fires on 12 of 2160 frames while his ordinary motion reaches p95 54.9, so it would start missing REAL cuts). Only one directive survives. | `docs/critic/phase-e.md` |
| E12 | 2026-09-02 | phase-e (T1/T3) | WRONG-NUMBER | 16c quotes `shreddit-media-ui` geometry ('absolute, z-index 2, pointer-events-none, sized exactly to the video box') from the locked-screen pass the same session declares worthless -- and the committed probe walks ancestors only, so it cannot produce that census at all. | Read `probe_player_hosts.py`; it never reads `getRootNode().children`. No raw census is committed. | CONFIRMED | RETRACTED IN PLACE in 16c, with the reason: "sized exactly to the video box" is a geometry claim and this section's own HONEST LIMITS say every rect in that pass is worthless -- quoting a rect three paragraphs above the sentence that voids it is the failure. The retraction also records that `probe_player_hosts.py` walks ANCESTORS and `shreddit-media-ui` is a SIBLING of the video, so no committed raw could contain that census; a real one needs `[...v.getRootNode().children]` on an UNLOCKED device. What survives is the layout-independent half the fix rests on (open root, `shreddit-media-ui` present, absolute, pointer-transparent). The platform-widening thread stays PARKED on his "fix youtube first" instruction; the retraction did not wait for it. | `docs/critic/phase-e.md` |
| E13 | 2026-09-02 | phase-e (T1/T3) | NIT | `bench/stretch-arm.js` sets `imageSmoothingQuality='high'` (the shipped path does not) and draws from an `<img>` rather than a `<video>`, so its ABSOLUTE `nm` magnitudes are not the shipped ones. | Read the bench against `init-entry.js`'s draw. | CONFIRMED | Annotated at the head of the bench, where anyone quoting a number reads it: both differences are named, and the bound is stated -- the ABSOLUTE nm magnitudes are not his device numbers and no threshold may be calibrated against them. What survives is the MATCHED-PAIR direction, because both arms see identical bytes through identical differences, so a per-face A-vs-B comparison cancels them. 16a is a paired test (17 of 18 faces, sign test p = 1.45e-4) and rests only on that half. | `docs/critic/phase-e.md` |
| E14 | 2026-09-02 | phase-e (T1/T3) | NIT | 16a says FOUR faces cross `NULL_MINT_NM_FLOOR`; the banked raw has three. | Count crossings in the banked JSON. | CONFIRMED | Corrected to THREE in `docs/engine-findings.md`. | `docs/critic/phase-e.md` |
| F1 | 2026-09-02 | phase-f (T1/T3) | WRONG-NUMBER | The commit message for `7dd3e03` states 'admissions 45 -> 58 (+28.9%) over 30 frames' as the letterbox result. That is an N=30 read of a bench whose own header warns a flat sweep at small N is a claim about the instrument. | Re-run `bench/movenet-gated.mjs` with N raised. | CONFIRMED | CONFIRMED, and retracted independently before the critic reported. At N=225 admissions are **373 against 373, exactly flat**; the more/less split is 47/44 (sign test p = 0.83). Findings 18 is the retraction, and it is also written at source in `detector.js` and `person-gate.mjs` and in the bench header. Loop 40's rule running the other way: **a LARGE effect at a small N is a claim about the sample.** What survives is 8 frames where only the letterbox admits anybody against 1 (p = 0.039). Raw `spikes/gauntlet/movenet-gated-n225.txt`. | `docs/critic/phase-f.md` |
| F2 | 2026-09-02 | phase-f (T1/T3) | EXPOSURE | `unpadPersons` clamps keypoints to [0,1]. `parsePersons` consumes keypoints as DIFFERENCES -- `headW = |lEar.x - rEar.x|`, `headH = headW * ar` -- and `HEAD_ANCHOR_UP` sets the patch top from that. A difference of clamped values is monotonically SMALLER, so clamping SHRINKS the head anchor and RAISES the top edge: hair and crown left sharp. | Feed a face whose ear lands outside the pad through both a clamping and a non-clamping `unpadPersons`, compare `headW` and the emitted box top. | CONFIRMED | CONFIRMED and fixed before the flag can ever ship. Measured on the critic's own fixture: headW 0.5133 -> 0.1600 and the patch top 0.0000 -> 0.3209 -- a third of the frame height of scalp uncovered. Keypoints are no longer clamped; the four BOX floats still are, because those are consumed as absolute positions and an out-of-frame box is meaningless. `test/unpad-persons.test.mjs` builds BOTH arms in-test and asserts the comparison rather than an absolute threshold, and it is red-proven against the clamping variant. | `docs/critic/phase-f.md` |
| F3 | 2026-09-02 | phase-f (T1/T3) | DEAD-CHECK | `control-triple.test.mjs` claims in its own header that it 'fails when ANY shipped constant in the decision layer moves'. It is blind to all of `person-gate.mjs`: `PATCH_MARGIN` 0.045 -> 0.500, `PERSON_MIN_SCORE` -> 0.99 and `HEAD_ANCHOR_UP` -> 0.0 all stay green with the cache verifiably rebuilt. | Move each constant, force the cache rebuild, run the test. | CONFIRMED | CONFIRMED. The cause is the instrument, not the test: **the corpus banks PARSED PERSONS -- boxes -- so it sits DOWNSTREAM of `parsePersons`**, and every constant whose only effect is inside that function is invisible because the function never runs. `PATCH_MARGIN` reaches the arm only through `personFromFace`, which fires where a banked observation has no box -- nowhere in the control arm. The claim is narrowed in place to what it covers (the ASSOCIATION AND DECISION layer, proven red on `PTRACK_ASSIGN`) and the blind spot is named with the benches that do cover it. A claim narrowed is better than an assertion widened until it is vacuous. | `docs/critic/phase-f.md` |
| F4 | 2026-09-02 | phase-f (T1/T3) | DEAD-CHECK | `movenet-gated.mjs`'s `outOfRange` sanity check -- the one that would catch a wrong inverse map, the failure worse than the defect -- reads boxes EMITTED by `parsePersons`, which clamps every box it emits. A map wrong by a factor of three still prints 'the inverse map holds'. | Multiply `sx` by 1/3 in the inverse and run the bench. | CONFIRMED | CONFIRMED. Red-proven exactly as described: with `sx/3` the old check read 0 out of range. The check now computes the UNCLAMPED inverse from the raw model floats and reports a **worst-overshoot magnitude, not a count**, because a hard count cries wolf on a CORRECT map: MoveNet's box regression reaches ~3.5px into its own black bar and the inverse divides the padded axis by sy = 0.5625, which amplifies that to 0.024 (two examples printed in the code, both y-only; x never overshoots because sx is 1.0 and there is nothing to amplify). Model noise is hundredths; a wrong map is 1.938. Tolerance 0.05, scoped to slots at or above `PERSON_MIN_SCORE`, and ZERO scored boxes checked exits 2 rather than reading clean. | `docs/critic/phase-f.md` |
| F5 | 2026-09-02 | phase-f (T1/T3) | WRONG-NUMBER | Findings 17's births table is headed 'Shipped bundle' and is the GREEDY arm; the optimal assignment shipped in the same session. Its cross-check ('147 in both genders') is contradicted by `iou-ladder-ceiling.txt` from the same session, which reports 141 man and 136 woman. | Run `births.mjs` at HEAD in both genders and compare against the IOU ladder. | CONFIRMED | CONFIRMED, and the root cause is worse than the label: **`births.mjs` ignored `GENDER` entirely** and printed `mode man` whatever it was handed, so its 'woman' row was the man arm relabelled -- the A-series ladder failure (one arm under two labels) in a bench that had it for as long as it has existed. `GENDER` is read now and an unrecognised mode exits 2 rather than defaulting. 17's table carries BOTH assignments with the greedy one labelled as such; with the bench fixed, `births.mjs` and the IOU ladder agree exactly at 141 man / 136 woman. The CONCLUSION is unchanged -- `birthContended` is the largest class on both assignments in both genders. Raw `spikes/gauntlet/births-1091.txt`. | `docs/critic/phase-f.md` |
| F6 | 2026-09-02 | phase-f (T1/T3) | DEAD-CHECK | `optimalAssign`'s `OPTIMAL_MAX_SIDE = 32` ceiling falls back to greedy SILENTLY, in the same commit range that added three counters citing 'a counter that does not exist reads exactly like a counter at zero'. Above the ceiling every optimal-arm number describes the greedy arm and nothing outside can tell. | Read `src/assign.mjs`: the fallback returns `greedyAssign` with no increment. | CONFIRMED | CONFIRMED. `assignFellBackGreedy` is bumped on the fallback, with the same guarded increment as `person-track`'s `bump`, duplicated rather than imported because person-track imports assign and the other direction is a cycle. `test/assign.test.mjs` asserts it fires above the ceiling AND does not fire at the ceiling exactly -- a counter that ticks on every ordinary frame means nothing. Red-proven by deleting the bump. | `docs/critic/phase-f.md` |
| F7 | 2026-09-02 | phase-f (T1/T3) | DEAD-CHECK | `whole-frame-counter.test.mjs`'s 'does not rebase an existing counter' test greps `init-entry.js` alone. The defect it names -- `clampFired`, loop 39 -- was a collision BETWEEN files (region-blur's patch clamp against body-clamp's). | Read the test: it matches against one file's source. | CONFIRMED | CONFIRMED. The sweep is the whole `src/` module tree now and the assertion is that exactly ONE file owns each name, with the per-file use-count kept on top of it. A second test pins that the sweep can fail, using `clampFired` -- still written by two modules -- as the live fixture, so this cannot silently become vacuous the way the first version was. | `docs/critic/phase-f.md` |
| F8 | 2026-09-02 | phase-f (T1/T3) | NIT | The emitted bundle at HEAD predates `7dd3e03` (0 hits for `unpadPersons` / `PERSON_LETTERBOX`) and its marker reads `764da03-dirty`, so the commit that set out to make the marker name a real commit did not. | Grep `app/src-tauri/gaze-page.js`. | CONFIRMED | CONFIRMED. Rebuilt. VERIFIED R15-STYLE IN THE EMITTED BUNDLE rather than by name, because the minifier renames everything: the letterbox branch is emitted as `resizeBilinear(jr(f,0),[s.dh,s.dw])` followed by the pad with `[s.dy, wn-s.dh-s.dy]` and `[s.dx, ...]`, and the flag it is gated on ships **`pde=!1`** -- off. So 1091's behaviour is unchanged and the code that would change it is present and inert. | `docs/critic/phase-f.md` |
| G1 | 2026-09-02 | phase-g (T1/T3) | WRONG-NUMBER | `extent-reach.mjs` re-implements the face-in-person test -- unpadded, and with no one-face-per-person rule. Through the shipped `faceInsideIndex` (10% pad per axis) plus the `claimed` loop the app falls to `personFromFace` for 317 of 1153 faces (27.5%), not the 194 (16.8%) findings 20 was written on. | Call the shipped rule instead of the private one and re-run the bench. | CONFIRMED | CONFIRMED. Reproduces exactly at 317/1153 (27.5%). Both halves of the private test were wrong and they pull opposite ways: the pad ADMITS faces a bare box misses, and `claimed` sends every SECOND face inside one box to `personFromFace` anyway. Fixed the way `crop-geometry.fitBox` was: `faceInsideIndex` and `synthFaceIndices` moved to `person-gate.mjs`, the app imports them, the bench imports them out of the EMITTED bundle. Findings 20 corrected in place, including the two figures carried forward into 21. Same defect class as G5 one finding earlier. | `docs/critic/phase-g.md` |
| G2 | 2026-09-02 | phase-g (T1/T3) | WRONG-NUMBER | Findings 21's 'it is height and it is occlusion' is a mechanism sentence written in the voice of a measurement. `ssdUnionH` exists and was not run. | Add `ssdUnionH` and a wide `ssdMinFaceW` arm to `mnbody-ab.mjs` and read the exposure column. | CONFIRMED | CONFIRMED and the sentence is WITHDRAWN. `ssdUnionH` recovers 0.5s of 30.5s (1.6%) man, 0.5s of 18.5s woman -- height is not the mechanism. And width past a point runs the OTHER way: `faceW 6.0` costs 142.0s of exposure (+119.5), six times the control, because a box wide enough to swallow the neighbour's observation takes the neighbour's track with it. Neither axis of the EXTENT explains the residual; what is left is where the box sits, and that is unmeasured and now said to be. | `docs/critic/phase-g.md` |
| G3 | 2026-09-02 | phase-g (T1/T3) | SCOPE | Every `mnBody` arm is byte-identical to CONTROL when MoveNet admits nobody -- which is 100% of his phone (findings 36, twelve slots n:0). And the CONTROL triple both raws print never builds `ssdBoxes`, so it is structurally blind to the body source it is printed beside. | Read the arm: every branch requires `ssdBoxes`, and `ssdBoxes` is nulled when `parsePersons` admits nobody. | CONFIRMED | CONFIRMED. 260 of 2160 corpus frames (12.0%) and every frame on his phone. Recorded as a scope caveat in findings 21, naming both halves: none of the body-source rows describe what he runs today, and the guard proves the DOWNSTREAM constants and nothing about the arm. | `docs/critic/phase-g.md` |
| G4 | 2026-09-02 | phase-g (T1/T3) | WRONG-NUMBER | `--test-concurrency=1` was shipped as the fix for the stale-cache race and its justifying comment says the suite passes on a correct change. It does not: serialising turns three failures into one, and costs ~30% of every run. | Touch a source constant and run the suite serialised. | CONFIRMED | CONFIRMED, both halves. Serialised reads 565 tests / 1 fail (not 0) because the first process to notice a stale bundle still throws by design, and 16.584s against 12.782s. The real fix is to get the rebuild in AHEAD of every test process: `pretest` runs `bench/_build.mjs`, which now exits 0 when it IS the entry point and throws only when imported. Concurrency back at the default. Measured after: 576/576 first run, 13.593s. The wrong justification is replaced at source rather than deleted. | `docs/critic/phase-g.md` |
| G5 | 2026-09-02 | phase-g (T1/T3) | DEAD-CHECK | F4's fix re-implements the inverse map and never calls `unpadPersons`, so it cannot detect a wrong shipped map -- and it is one-sided, checking only overshoot. | Break `unpadPersons` in both directions and run the bench. | CONFIRMED | CONFIRMED. The bench now computes an independent inverse AND compares the shipped `unpadPersons` output against it, two-sided, at DEV_TOL 1e-6, alongside the existing overshoot magnitude. Red-proven both ways -- growing the map reads 6.48e-1 and shrinking it 6.53e-1, both exit 2 -- and green at 2.65e-8. Same class as G1 and G9, same remedy: call the shipped code, do not model it. | `docs/critic/phase-g.md` |
| G6 | 2026-09-02 | phase-g (T1/T3) | DEAD-CHECK | The same bench exits 0 while printing 'THE INVERSE MAP IS WRONG'. A gate that reports a failure in prose and a pass in its exit code is not a gate. | Run it with a broken map and read the exit code. | CONFIRMED | CONFIRMED. Both failure modes -- overshoot above tolerance and deviation above tolerance -- set exit 2, and so does a vacuous run with zero scored boxes checked. | `docs/critic/phase-g.md` |
| G7 | 2026-09-02 | phase-g (T1/T3) | WRONG-NUMBER | Findings 21a's 'the arm is structurally four times more active in his mode' rests on `mnEdgeInert`, which fires whether the branch found a cleared neighbour and declined to move OR found none at all. The second dominates, so the ratio measures how often men clear, not how active the arm is. | Split the counter into 'opportunity' and 'no eligible neighbour' and read the per-opportunity rate. | CONFIRMED | CONFIRMED and the conclusion REVERSES. `mnEdgeOpportunity`/`mnEdgeNoNeighbour` added: the branch runs 1014 times in both arms, with an eligible neighbour 348 man / 84 woman, none 666 (65.7%) / 930 (91.7%) -- and per OPPORTUNITY the edge moves 81.9% man against 79.8% woman, two points apart. So a woman-mode measurement of adjacency work is UNDER-POWERED, not structurally inert, and the fix is more woman-mode frames with neighbours rather than discounting the arm. 21a corrected in place. | `docs/critic/phase-g.md` |
| G8 | 2026-09-02 | phase-g (T1/T3) | DEAD-CHECK | F6's `assignFellBackGreedy` is never seeded, so 0 cannot be told from never-hooked -- the exact ambiguity the counter was added to remove, and the one loop 34 named. | Read the emitted report on a run that never hits the ceiling: the key is absent. | CONFIRMED | CONFIRMED. Seeded to 0 at the one call site that knows the optimal assignment is about to run, and ONLY in optimal mode -- in greedy mode a constant 0 would read as a measurement. Seeded in `person-track.mjs` rather than `assign.mjs` so the meaning is 'the optimal assignment ran and never hit its ceiling'. | `docs/critic/phase-g.md` |
| G9 | 2026-09-02 | phase-g (T1/T3) | DEAD-CHECK | F7's counter-collision sweep matches the bare NAME anywhere in a file, so three comment lines in `init-entry.js` explaining that `clampFired` was taken count as ownership -- and its red-proof fixture demonstrates only that a twice-MENTIONED name trips the sweep, never a twice-WRITTEN one. | Read the sweep: it is a substring match, and `clampFired` has exactly one writer. | CONFIRMED | CONFIRMED, and the fixture proved the bug rather than the check. Comments are stripped first and only a bump SITE counts. The rule is STRUCTURAL, not a helper-name enumeration -- a `life` property, a quoted literal on a bump-shaped call (`bump*` or `*Life`), or a seed to 0 -- because the first two attempts reported ZERO owners for `clampFired` itself, which bumps out of a ternary, and missed `init-entry`'s local `wholeFrameLife`. The sweep is an exported pure function now and the red proof is synthetic: two writers caught, a comment not an owner, a seed counted, and the live tree agreeing (`clampFired` -> exactly `person-track.mjs`). | `docs/critic/phase-g.md` |
| G10 | 2026-09-02 | phase-g (T1/T3) | NIT | Findings 21a miscounts its own banked raw twice: three windows improve false cover, not two; and two windows' phantom rises in the woman arm, not one. | Count the rows in `spikes/gauntlet/mnedge-where.txt`. | CONFIRMED | CONFIRMED, both. Corrected in place with the miscount recorded rather than edited away, since the raw is banked beside it. (The `arch-arms.mjs:731` citation in findings 20 was checked too and is a line-number drift, not a wrong file.) | `docs/critic/phase-g.md` |
| G11 | 2026-09-02 | phase-g (T1/T3) | NIT | The in-code price of F4's red proof reads 0.699; the real figure at the N the bench now runs is larger. | Break `sx` to a third and read the printed magnitude. | CONFIRMED | CONFIRMED. It is 1.938, measured at N=4 and N=10; 0.699 was a partial run. Corrected at source, where anyone calibrating the tolerance reads it. | `docs/critic/phase-g.md` |
| G12 | 2026-09-02 | phase-g (T1/T3) | NIT | F2's justification -- 'keypoints are consumed as DIFFERENCES, not as positions' -- is too strong. They are also unioned as absolute positions and `headX`/`headY` are emitted from them, so unclamped can emit a coordinate outside 0..1 (-0.00067 measured). | Read `parsePersons`'s keypoint consumers. | CONFIRMED | CONFIRMED, and the DECISION is unchanged. Both absolute consumers are monotone in the COVERING direction -- a union with an out-of-frame point can only grow the box, a head anchor placed marginally high can only raise a patch top -- so unclamped is right for both reasons. The comment now says 'every consumer is monotone toward covering' instead of 'no consumer reads a position'. Inert today either way: the flag ships off. | `docs/critic/phase-g.md` |
| H1 | 2026-09-02 | phase-h (T1/T3) | WRONG-NUMBER | `bodyFromSsd` (`bench/arch-arms.mjs`) never carried `headX`/`headY`/`headW`/`headH`, where `personFromFace` and `boundBodyToSlot` both do -- so `sameHuman`'s head-separation guard was off for every mnBody/ssd observation and it merged any two boxes 60% contained, including two different people handed the same MoveNet person box. Findings 21/23's cost figures describe that bug, not the box. | Add the four head fields to `bodyFromSsd`, exactly as `personFromFace` computes them, and re-run `mnbody-ab.mjs`/`mnbody-births.mjs`. | CONFIRMED | CONFIRMED. Fixed at source (`bench/arch-arms.mjs`). Re-run: G2's excess shrinks from +30.5s (man) / +18.5s (woman) to **+1.5s / +2.0s**; `ssdUnionH` now recovers 100% of the man excess and 0% of the woman excess (not 1.6%); `faceW 6.0` now REDUCES exposure (-2.5s man / -13.5s woman) instead of costing +119.5s. Births move 141->139 man / 136->133 woman, `birthContended` RISES to 47.5%/49.6% (was 42.6%/45.6% on the synthetic guess) -- the measured body gives the assignment MORE to contend over, not less. Section 21, section 23, section 7 of `docs/engine-findings.md` and phase-g's G2 corrected in place (G2 via an appended CORRECTED annotation, history kept). | `docs/critic/phase-h.md`, `docs/engine-findings.md` sec 21/23, `docs/critic/phase-g.md` G2 |
| H2 | 2026-09-02 | phase-h (T1/T3) | EXPOSURE | `sameHuman` (`src/person-track.mjs`) merges on containment ALONE whenever EITHER box lacks a numeric `headX` -- including a headless MoveNet person box sitting on top of a DIFFERENT headed person. | Call `sameHuman` with a headed box and a headless box whose containment passes but whose head does not land inside the other. | CONFIRMED | CONFIRMED. Fixed at source: when exactly one side has a head, that head's own centre must land inside the OTHER box before they merge; only when NEITHER side has anything to separate on does containment alone still decide (counted now, `dedupeHeadUnknown`). Three new tests in `test/person-track.test.mjs`, red-proven by reverting the fix (101 pass / 2 fail of 103) and green restored (103/103). | `docs/critic/phase-h.md`, `app/gaze/src/person-track.mjs` |
| H3 | 2026-09-02 | phase-h (T1/T3) | DEAD-CHECK | `bench/critic-packet.mjs`'s own test-oracle step calls `node --test` directly, bypassing `npm`'s `pretest` (`bench/_build.mjs`) -- the one path G4's stale-cache fix does not cover. `.claude/skills/gauntlet-round/SKILL.md:194` had the same bypass. | Edit `src/` without rebuilding, run the packet's oracle step, read the output. | CONFIRMED | CONFIRMED. `critic-packet.mjs` now runs `bench/_build.mjs` directly before the oracle, same rebuild `pretest` runs; a build failure is reported as its own labelled failure. `SKILL.md` step 5 now runs `npm test` (not raw `node --test`) with a one-line note explaining why. | `docs/critic/phase-h.md`, `app/gaze/bench/critic-packet.mjs`, `.claude/skills/gauntlet-round/SKILL.md` |
| H4 | 2026-09-02 | phase-h (T1/T3) | DEAD-CHECK | `test/assign.test.mjs` claims a `test/assign-seed` test pins G8's seed -- no such test exists anywhere in the tree. Deleting the seed block in `person-track.mjs` leaves the suite fully green. | Delete the seed block, run the suite. | CONFIRMED. Red-proven: with the seed block removed the existing suite still passed everywhere (never covered). | CONFIRMED | Added a test to `test/assign-wired.test.mjs` -- the only file that reaches the seed, since it lives inside `updatePersonTracks` and `assign.test.mjs` calls `optimalAssign` directly. Red-proven by removing the seed block (assertion fails: expected 0, actual undefined) and restoring it (108/108 green across both assign test files). The stale comment in `assign.test.mjs` corrected to point at the real test. | `docs/critic/phase-h.md`, `app/gaze/test/assign-wired.test.mjs`, `app/gaze/test/assign.test.mjs` |
| H5 | 2026-09-02 | phase-h (T1/T3) | DEAD-CHECK | `ownersOf` (`test/whole-frame-counter.test.mjs`) still misses three shapes of a real write (a same-behaviour local helper not named `bump*`/`*Life`; an alias of the `life` bag; a key held in a local constant) and wrongly flags two shapes of a mention as a write (a comment right after a colon; the name inside a plain sentence near `: 0`). | Run the sweep against seven synthetic fixtures A-G (appendix `ownersprobe.mjs`) and read which of B-G disagree with what they should be. | CONFIRMED, all six (B-G except the acknowledged limit). Red-proven against the committed `ownersOf`: B/C/D missed, E/F wrongly flagged. | CONFIRMED | Rewrote `ownersOf`: tracks local helpers that write a parameter into `life[...]` (B), aliases of the `life` bag (C), and local constants holding the literal name (D); the comment-stripper's colon guard removed (E); the seed-fallback narrowed to a property-key position, `{`/`,`/`(` immediately before the name (F). Case G (an unrelated object literal sharing the same key at 0) is a documented, unfixed KNOWN LIMIT. New test pins all seven; existing tests (real-tree sweep, two-writer/comment/seed fixture, `clampFired`) still green. | `docs/critic/phase-h.md`, `app/gaze/test/whole-frame-counter.test.mjs` |
| H6 | 2026-09-02 | phase-h (T1/T3) | DEAD-CHECK | The 'one copy' test in `test/face-in-person.test.mjs` names an exact syntax shape. A private, unpadded re-derivation written as an arrow function under a suffixed name (`faceInsideIndex2`) plus a re-sorted `order.sort((a,b)=>...)` passes both checks, imports and all -- appendix `onecopy.mjs`. | Apply the mutation in memory, run the four assertions. | CONFIRMED. Red-proven directly against `src/init-entry.js`: the pre-fix test's two `doesNotMatch` checks both passed against the exact bypass mutation. | CONFIRMED | Broadened both checks past a single syntax shape: any identifier starting with `faceInsideIndex`/`faceOrderBySize`/`synthFaceIndices` but not exactly that name is refused; `order.sort(` is refused outright, since `order` is already the sorted output of the import. Red-proven by applying the critic's exact mutation to a scratch copy of `init-entry.js` (fails: `faceInsideIndex2` caught) and restoring (7/7 green). KNOWN LIMIT documented: a re-derivation under a wholly unrelated name still passes. | `docs/critic/phase-h.md`, `app/gaze/test/face-in-person.test.mjs` |
| H7 | 2026-09-02 | phase-h (T1/T3) | WRONG-NUMBER | `docs/engine-findings.md` section 23 quoted `birthContended` at 44-51%, which is section 17's GREEDY (1090) row -- section 23's own control is the 1091 OPTIMAL arm, whose real figure is 42.6%/45.6%. | `node bench/births.mjs \| head -8`. | CONFIRMED. `births.mjs` reproduces 141/38/43/60 (42.6%), matching `mnbody-births`'s control row exactly. | CONFIRMED | Already resolved as part of H1's rewrite of section 23 (the stale 44-51% cross-reference was replaced with the corrected 42.6%/45.6% figures and the withdrawal of the 'assignment looks cheaper' claim). Verified no remaining 44.2%/51.0% reference in the doc is mislabelled -- the two that remain are correctly tagged `greedy (1090)`. | `docs/critic/phase-h.md`, `docs/engine-findings.md` sec 23 |
| H8 | 2026-09-02 | phase-h (T1/T3) | NIT | `docs/engine-findings.md` section 20's 'Per window it runs 45.0% to 100.0%' sits directly under the per-corpus CLAIM-rate rows and reads as a per-window bound on them. It is the per-window ADMIT-FRAME rate, a different quantity with a different denominator. | `node bench/extent-reach.mjs \| tail -20` -- the only per-window column is `admitFrames/frames`. | CONFIRMED. | CONFIRMED | One clause plus an aside naming the mixup: 'the ADMIT-FRAME rate above -- the share of FRAMES ... not the share of FACES that claim a box -- runs 45.0% to 100.0% of frames'. | `docs/critic/phase-h.md`, `docs/engine-findings.md` sec 20 |
| H9 | 2026-09-02 | phase-h (T1/T3) | NIT | `bench/_build.mjs:110`'s entry-point test is a case-SENSITIVE string compare. Windows paths are case-insensitive, so a differently-cased invocation of the same file takes the IMPORT branch and throws instead of exiting 0. | `touch src/tuning.mjs && node bench/_BUILD.mjs`. | CONFIRMED. Red-proven by swapping in the pre-fix source under the uppercase filename: throws, exit 1. | CONFIRMED | `fs.realpathSync` on both sides, case-folded only on `process.platform === 'win32'`, falling back to the old compare if `realpathSync` throws. `node bench/_BUILD.mjs` now exits 0 and rebuilds exactly like the lowercase invocation. | `docs/critic/phase-h.md`, `app/gaze/bench/_build.mjs` |
| H10 | 2026-09-02 | phase-h (T1/T3) | NIT | `bench/movenet-gated.mjs`'s 'THE INDEPENDENT INVERSE' comment oversells what the check is: the four expressions and the clamp are line-for-line what `unpadPersons` contains, from the same `FIT` and the same `S` -- a second typing of the same arithmetic, not an independent derivation. | Diff the two; the reported 2.65e-8 residual is one ULP of Float32 storage, not a difference in method. | CONFIRMED (a labelling correction, not a behavioural claim). | CONFIRMED | Comment rewritten to 'A SECOND TYPING OF THE SAME INVERSE' plus an explicit statement of what it can and cannot catch. No behaviour change; the check stays red-proven both directions per G5/G6. | `docs/critic/phase-h.md`, `app/gaze/bench/movenet-gated.mjs` |
| I1 | 2026-09-02 | phase-i (bench) | WRONG-NUMBER | `bench/arch-arms.mjs`'s `HIS_EFFZOOM = 2000` / `K_HIS = 3` were hand-picked literals justified by a cap that Task 3 (VERDICT_DUTY 4->2) and Tasks 1/2/4's verdict-cost cuts removed; every corpus number in the repo, `test/control-triple.test.mjs`'s self-check and the pending coast-dial ruling rest on them. | Re-run CONTROL at the device's measured told (1589.4ms): the triple moves on every axis (man +4.0/-7.5/-105.5s; woman +3.0/-7.5/-110.0s). Falsifier fails -- the baseline is not insensitive to this. | CONFIRMED | Fixed at source (commit `7423f82`). `HIS_EFFZOOM` and `K_HIS` are now DERIVED -- `HIS_EFFZOOM` from the shipped `cadence.mjs` constants plus a banked device measurement (`bank/his-regime.json`, Redmi M2010J19SI, man mode, 150s), `K_HIS` from that banked verdict gap rounded to the nearest 500ms bank interval -- rather than re-implemented literals. `test/his-regime.test.mjs` pins the derivation within 5% of the banked number and cross-checks the json against its own cited source file. CONTROL moved: man 22.5/136.5/547.5 -> 13.5/117.5/477.5, woman 25.5/201.5/628.0 -> 15.0/181.0/569.5, cross-checked against the critic's own independently-computed table. CLAUDE.md's self-check sentence and every bench that cites CONTROL (verified today: `bench/gender-skip-arm.mjs` reproduces 13.5/117.5/477.5 man, 15.0/181.0/569.5 woman) now agree. | `docs/critic/phase-i.md` I1, `app/gaze/bench/arch-arms.mjs`, `bank/his-regime.json`, `app/gaze/test/his-regime.test.mjs` |
| I2 | 2026-09-02 | phase-i (T4) | EXPOSURE | Task 4's gender-read-skip was accepted on an exposure delta of +1.0s (man) / -1.5s (woman) measured at `hisRegimeOpts(g)` (told 2000, K=3) -- the regime I1 shows the device has left. At the corrected K=2 regime the woman-mode delta is +4.0 to +4.5s against a stated +1.0s budget (4x over), and false cover/phantom (+21-34s / +7-42s in every regime) were never gated at all. | Run the arm at the corrected told/stride and show the woman delta stays <= +1.0s: it is +4.0s at K=2/told 1589 and +4.5s at K=2/told 1201. Falsifier fails. | CONFIRMED | Not raised to him yet, so shipped INERT rather than tuned on the arm's own say-so: `GENDER_REFRESH_MS` default 2000 -> 0 in `person-track.mjs` and `rules/tuning.json`; the OTA clamp in `tuning.mjs` widened `[1000,4000]` -> `[0,4000]` (0 = every track read every pass, i.e. byte-identical to CONTROL -- the arm's own red-proof asserts this). The `tuning.mjs` comment above the SPEC entry now quotes I2's numbers verbatim (man skip@2000 14.5/140.5/484.5 vs CONTROL 13.5/117.5/477.5; woman 19.0/206.5/603.5 vs 15.0/181.0/569.5 -- the woman exposure delta is +4.0s against the +1.0s budget, 4x over) so the next session sees the table before pushing 2000 live. `test/track-needs-read.test.mjs`'s shipped-default test pins the literal 0 (also closes I12). Re-run today: man 14.5/140.5/484.5 (CONTROL 13.5/117.5/477.5), woman 19.0/206.5/603.5 (CONTROL 15.0/181.0/569.5) -- exact match to the cited table. Not done: widening the arm's own acceptance block to gate false cover/phantom -- that changes the acceptance CONTRACT and belongs to whoever next rules on the constant, not to shipping it inert. | `docs/critic/phase-i.md` I2, `app/gaze/src/person-track.mjs`, `app/gaze/src/tuning.mjs`, `rules/tuning.json`, `app/gaze/bench/gender-skip-arm.mjs` |
| I3 | 2026-09-02 | phase-i (T1) | DEAD-CHECK | Task 1's whole position-pass skip gate in `sampleOnce` (init-entry.js) is untested: `personsLive` appears in exactly one test file and never loads init-entry.js, `positionPassSkipped` appears in zero tests, and the gate -- `sampling = false;` included, the line that separates a skipped pass from a permanently frozen player -- can be deleted outright with the suite fully green. | Delete the 5-line gate from init-entry.js: `npm test` stays 633/633. Falsifier fails. | CONFIRMED | `test/person-skip-live.test.mjs` gained a structural test anchored on the `wasVerdict` declaration through the `verdictBusy` block, comments stripped first (the same G9 shape I4 and `person-skip.test.mjs`'s loadstart test already guard against), matching the whole gate as one ORDERED block: `if (isPlayer && !wasVerdict && !personsLive()) { bumpLife('positionPassSkipped'); sampling = false; return; }`. Red-proved: the block deleted from a scratch copy of init-entry.js, test went red (1 fail of the new tests), restored and `git diff --stat` confirmed byte-identical to the pre-mutation state. | `docs/critic/phase-i.md` I3, `app/gaze/test/person-skip-live.test.mjs`, `app/gaze/src/init-entry.js` |
| I4 | 2026-09-02 | phase-i (T4) | DEAD-CHECK | `track-needs-read.test.mjs`'s "bumps genderReadSkipped inside the verdict branch" test locates the skip with `src.indexOf(...)` / `src.includes('trackNeedsRead(')` on RAW source, so it is satisfied by a commented-out call -- the exact shape phase-g G9 named and `person-skip.test.mjs` already strips comments to avoid. | Comment out the whole Task 4 skip block in init-entry.js: the file's own test AND the whole suite (633/633) stay green. Falsifier fails. | CONFIRMED | The test now strips block and line comments off the sliced region before matching `bumpLife('genderReadSkipped')` and `trackNeedsRead(`. Red-proved: the whole Task 4 skip block commented out in a scratch copy, test went red, restored. | `docs/critic/phase-i.md` I4, `app/gaze/test/track-needs-read.test.mjs` |
| I5 | 2026-09-02 | phase-i (T4) | DEAD-CHECK | `gender-skip-arm.mjs`'s red-proof at GENDER_REFRESH_MS=0 cannot fail: the refresh check is unconditionally true at 0, so the skip branch is never entered, and the check proves only that the wrapper's `{...o, at: nowMs}` spread is transparent -- nothing about the conversion the wrapper performs when the skip DOES fire. | Break the skip path deliberately (return the skipped observation without its `at` field, a real fidelity bug) and show the @0 arm notices: it still reports CONTROL match / fired=0 in both modes. Falsifier fails. | CONFIRMED | Fixed at source (commit `7423f82`). The bench now asserts BOTH halves and exits 2 on failure rather than eyeballing either: the @0 run must reproduce CONTROL byte-for-byte AND fire zero times (unchanged), and the shipped run (2000ms) must fire at least once AND its resulting triple must differ from CONTROL on at least one column. Re-verified today after I1/I2/I6's changes: shipped run fires 550/1619 (man) and 600/1627 (woman), both nonzero, both triples differ from CONTROL on every column. | `docs/critic/phase-i.md` I5, `app/gaze/bench/gender-skip-arm.mjs` |
| I6 | 2026-09-02 | phase-i (T4) | WRONG-NUMBER | `gender-skip-arm.mjs` prints `fired=345` with no denominator, so the corpus rate cannot be compared to the device's `genderReadSkipped 18` over 117 verdicts -- and the rate is a STEP FUNCTION of the bench's own verdict spacing (`dt*stride`), not a smooth property of the constant: 0.9% at refreshMs=1000, 28.3% at 2000, a 31x step across one OTA range. | Show the rate is stable across the dial. It is 0.9% at 1000 and 28.3% at 2000. Falsifier fails. | CONFIRMED | Added a `skipEligible` counter -- incremented whenever an observation matched an existing track by IoU, i.e. was actually subject to the `trackNeedsRead` check, the correct denominator since an unmatched observation can never be skipped regardless of the constant -- printed as `fired=N/M (rate%)` beside both the @0 and shipped rows. Also prints the arm's own matched-observation read spacing (`dt*K`) and restates GENDER_REFRESH_MS as a count of verdict intervals at that spacing (e.g. "GENDER_REFRESH_MS=2000 is 2.00 verdict intervals" at today's corrected K_HIS=2 from I1), the relative-contract framing the FIX asked for. Verified today: man fired=550/1619 (34.0%), woman fired=600/1627 (36.9%) -- both now readable and comparable, where the original commit body only ever printed the numerator. Not done: rewiring `probe_latency_ab.py`'s own device-side denominator, or restating `tuning.mjs`'s SPEC comment itself in verdict-interval units -- both left for whoever next tunes this constant, since the defect the row names (an unreadable, non-comparable published rate) is closed by this print. | `docs/critic/phase-i.md` I6, `app/gaze/bench/gender-skip-arm.mjs` |
| I7 | 2026-09-02 | phase-i (device row) | WRONG-NUMBER | `probe_latency_ab.py` counts passes DROPPED by the epoch guard as verdicts (both pushed to the same `.finally()` ring), and a dropped pass zeroes `lastZoomAt` so the very next pass is a near-zero-gap verdict -- so "verdict gap 2075 -> 1201ms" mixes cadence gaps with drop-forced and cut-forced ones in proportions that differ between the two arms (stage A: 19 drops + 34 cuts / 117; 1091: 23 + 35 / 74). | Show `stages` excludes dropped passes: `stage.v = wasVerdict ? 1 : 0; dbgSt.stages.push(stage);` sits inside `.finally(...)`, downstream of the `myEpoch !== passEpoch` early return. It does not exclude them. Falsifier fails. | CONFIRMED | Fixed at source (commit `7423f82`). `init-entry.js` is read-only for this fix, so it reads the existing `mark('tracks')` signal instead of adding a new one: a dropped pass never reaches that mark, so `typeof e.tracks === 'number'` is the correct "useful" predicate. `probe_latency_ab.py`'s output now splits `verdicts` (useful) / `verdictsDropped`, keeps the old total as `passesAll`, and gaps/`secsPerVerdict` are computed over useful verdicts only (`secsPerVerdictUseful` added alongside the old figure). Existing banked jsons (`latency-ab-1091.json`, `latency-ab-stageA.json`) are left as-is -- they cannot be re-derived offline -- and the new `vAt`/`dropped` series bank going forward. Corrected reading: the direction of the improvement survives (98 useful verdicts vs ~51, i.e. 1.53s per useful verdict against 1091's 2.94s) -- it is the raw "2075 -> 1201ms" digits that do not. | `docs/critic/phase-i.md` I7, `spikes/gauntlet/probe_latency_ab.py` |
| I8 | 2026-09-02 | phase-i (track-timeline) | EXPOSURE | `track-timeline.mjs`'s "only in B" branch (a track born mid-interval) returns B's box UNLERPED for the whole back-dated segment -- a moving subject is covered at their arrival position and sharp at their real position for up to one verdict interval, exactly the interval the delay line exists to remove. The symmetric "only in A" branch (a detector miss) has the same shape. Not yet shipped: Task 10 never wired a caller to `boxesAt`. | Point at the lerp on the only-in-B branch: there is none (`lerpBox` is called only in the both-branch). Falsifier fails. | CONFIRMED | Fixed at source (commit `7423f82`), before any caller reached `boxesAt`. `BIRTH_BACKDATE_PAD` (0.15 of the box's own width/height per side, clamped to the timeline's [0,1] domain) pads the box outward on both the only-in-B and only-in-A branches, covering the SWEPT region rather than the endpoint -- patches stay solid, one rectangle, shrinking linearly to zero as the birth verdict is approached. Two new tests (interpolated padding, domain clamp), red-proved against the unpadded box. | `docs/critic/phase-i.md` I8, `app/gaze/src/track-timeline.mjs` |
| I9 | 2026-09-02 | phase-i (delay-presenter) | EXPOSURE | `delay-presenter.mjs`'s ring is sized from a hard-coded `ASSUMED_FPS=30`; on a 60fps stream (the device's stage-A run decodes 1280x720 at 60fps) `pickPresent` returns -1 on every tick, the presenter never leaves `'refilling'`, and the WHOLE VIDEO is covered permanently with no recovery path. | Point at a measurement of fps, or a recovery path from a sustained `pick < 0`. Neither exists in the module. Falsifier fails. | CONFIRMED | Fixed at source (commit `7423f82`), both halves the critic named. (a) The presenter now measures fps from `requestVideoFrameCallback` `mediaTime` deltas (median of a rolling window, falling back to `ASSUMED_FPS` under 10 samples) and re-derives `ringBudget` when measured fps drifts more than 20% from what it is currently sized for. (b) `presentTick` never leaves the picture permanently covered when the ring is at its eviction cap and the oldest entry is still newer than target -- it presents the oldest entry anyway and counts it via a new `stats.delayCollapsed`, rather than looping `stats.late` forever. Two new tests (60fps ring growth past the 30fps-assumed cap; collapse serving the oldest entry once permanently stuck), each red-proved against a disabled copy of its own mechanism. The pre-existing "ring never exceeds budget" test asserted the OLD buggy behaviour (permanently stuck, 0 presentations) and is rewritten to assert the new one. | `docs/critic/phase-i.md` I9, `app/gaze/src/delay-presenter.mjs`, `app/gaze/src/delay-core.mjs` |
| I10 | 2026-09-02 | phase-i (T2) | EXPOSURE | Task 2's `PERSON_SKIP_EVERY=4` cost is unmeasurable by either instrument the repo owns (the corpus CONTROL arm never builds `ssdBoxes`; the device's own footage reads `slotsN [0,0,0]` in both arms) and the commit does not say so. On the device MoveNet backs off to once every ~5.4s, and nothing in the scene-cut branch reset that independent back-off cycle -- a held "nobody admitted" answer could outlive the shot it was measured in. | Show a corpus arm in which PERSON_SKIP_EVERY changes a number: every `ssdBoxes` construction site is gated on options `hisRegimeOpts` never sets. The falsifier cannot be constructed with CONTROL. | CONFIRMED | The row's own FIX is conditional ("if it does not [already reset on a cut], add it"): confirmed from source that the scene-cut branch in `gateTick` (`bumpLife('cutDetected')` ... `heldPersons = []` ... `passEpoch++`) did NOT touch `person-skip.mjs`'s module state before this fix. Added `resetPersonSkip()` between `heldPersons = []` and `passEpoch++`, with a comment recording the measured ~5.4s window and the mechanism (`PERSON_EMPTY_STREAK + (PERSON_SKIP_EVERY-1)` passes of old-shot evidence). This closes the compounding the row names -- a held answer outliving its own shot -- but the row's broader finding, that PERSON_SKIP_EVERY's cost is unpriceable by either instrument on footage the corpus/device sample today, stands as written: pricing it needs a clip selected for occluded/back-turned subjects, out of scope for a source fix. Structural test added (`person-skip-live.test.mjs`, anchored on the cut branch's own `bumpLife('cutDetected')` ... `passEpoch++` span, comments stripped), red-proved by removing the call from a scratch copy. **RE-PRICED ON THE REDMI (stageB2/B3/B4, 2026-09-02): the reset ran MoveNet 3x as often on footage that cuts every ~5s (personPassSkipped 89 -> 29-33, verdict gap 1201 -> 2068/2130ms) and even ONE forced look per cut (forcePersonLook) cost personPassSkipped 89 -> 50, verdict p50 705 -> 991, gap 1201 -> 1998 -- every look admitted nobody (slots n:0). The guard is an exposure-for-cadence trade (a MoveNet-only subject entering with a cut while backed off) and it is his: shipped as `CUT_PERSON_LOOK` on the OTA channel [0,1], DEFAULT 0 (stage A behaviour); unit tests pin off = inert and on = one look, back-off kept.** | `docs/critic/phase-i.md` I10, `app/gaze/src/init-entry.js`, `app/gaze/src/person-skip.mjs`, `app/gaze/test/person-skip-live.test.mjs` |
| I11 | 2026-09-02 | phase-i (T1) | WRONG-NUMBER | `lastPassMs` has exactly one write site (the position-pass branch of the `.finally`), and Task 1's gate stops that branch from ever running once `personsLive()` is false -- confirmed on device (`positions: 0` for the whole 150.1s window). `effInterval` on the player therefore derives from a race (0, if the back-off started before a position pass ever completed, vs a real cost) rather than a live value. | Find a second write to `lastPassMs`: one declaration, one read, one write, and the write is unreachable in his regime. Falsifier fails. | CONFIRMED | Took the FIX's second option. `effInterval` now reads `personsLive() ? Math.min(POSITION_MAX_INTERVAL_MS, Math.max(floor/rate, lastPassMs*POSITION_DUTY)) : POSITION_MAX_INTERVAL_MS` -- once positions are being skipped the floor falls back to the design ceiling explicitly, rather than deriving from a `lastPassMs` that can no longer be written (the old expression's `0 * POSITION_DUTY` race). `lastPassMs` and `effInterval` are now banked into `dbgC.tuning` (the same bag `coastMs`/`toldMs` already ride) every pass, numeric, so the cadence row can read them instead of depending on an unrecorded value; `probe_latency_ab.py` itself was not extended to print them -- that file is concurrently owned by other in-flight work (Task 11's --delay arm) and is out of this fix's declared scope, but the values are live in `window.__TS_GAZE_IDS.tuning` for whoever next extends that probe. Two structural tests added (the fallback expression; the two banking assignments), both comment-stripped, both red-proved by reverting the change in a scratch copy. | `docs/critic/phase-i.md` I11, `app/gaze/src/init-entry.js`, `app/gaze/test/person-skip-live.test.mjs` |
| I12 | 2026-09-02 | phase-i (T4) | NIT | `GENDER_REFRESH_MS`'s shipped default is not pinned to a literal anywhere in `track-needs-read.test.mjs` -- both existing tests compute their own expectations from the constant, so moving the source default to 4000 and regenerating `tuning.json` leaves the file fully green, the exact shape sibling constants `PERSON_SKIP_EVERY` and `VERDICT_DUTY` were already rewritten to guard against. | Find a literal `2000` pinned against GENDER_REFRESH_MS in `app/gaze/test/`: only the two derived comparisons exist. Falsifier fails. | CONFIRMED | Folded into the I2 fix rather than a separate change: `track-needs-read.test.mjs` gained `assert.match(src, /^export var GENDER_REFRESH_MS = 0;/m, ...)` against `person-track.mjs`'s source. The literal is 0, not 2000, because I2's fix moved the shipped default itself (see I2's row) -- the test pins whatever the shipped default now is, so moving it silently (the exact gap this row names) now fails this assertion directly. | `docs/critic/phase-i.md` I12, `app/gaze/test/track-needs-read.test.mjs`, `app/gaze/src/person-track.mjs` |
| I13 | 2026-09-02 | phase-i (T3) | NIT | `cadence-duty.test.mjs`'s claim-site check runs on raw source, so it is satisfied by a comment (the same G9 shape as I4); its `restore` helper is `applyTuning({ VERDICT_DUTY: cadence.VERDICT_DUTY === 2 ? 2 : 2 })` -- a ternary with identical branches, dead code in a file about not shipping dead constants. | Neutralise the claim site (hardcode `lastVerdictMs * 4`, keep the original expression as a trailing comment) and show the test stays green: 4/4 pass. Falsifier fails. | CONFIRMED | The claim-site assertion now matches against a comment-stripped copy of `init-entry.js` before matching. `restore` is `applyTuning({ VERDICT_DUTY: 2 })`, the dead ternary removed. Red-proved: the claim site rewritten to a hardcoded `lastVerdictMs * 4` with the original expression kept as a trailing comment in a scratch copy -- test went red, restored. | `docs/critic/phase-i.md` I13, `app/gaze/test/cadence-duty.test.mjs` |
| I14 | 2026-09-02 | phase-i (delay-presenter) | NIT | `delay-presenter.test.mjs`'s "detach after frames presented" test never presents a frame (`video.currentTime` stays 0, `presentTarget` is negative, `pickPresent` always misses and the `stats.late++` path runs instead), and its third assertion (`presentedMediaTime() === null` after detach) cannot fail either way since `detach()` unconditionally nulls it. | Show the test presents a frame (advance `currentTime`, or read `stats().late`): as written `stats().late` is 1 and `stats().presented` is 0. Falsifier fails. | CONFIRMED | Fixed at source (commit `7423f82`). Rewritten to advance `video.currentTime` past the delay so a real pick lands, assert `presented===1` and `presentedMediaTime()` non-null BEFORE `detach()`, and assert `canvas._ctx.calls` actually recorded a `drawImage` -- red-proved by forcing `pick=-1` always. | `docs/critic/phase-i.md` I14, `app/gaze/test/delay-presenter.test.mjs` |
| I15 | 2026-09-02 | phase-i (T2) | NIT | `person-skip.mjs`'s `emptyRun`/`skipsSince` are module-global, and Task 1 gives that global authority over a new per-video decision -- on a page with two attached videos, one's MoveNet emptiness would silence the other's position passes. | Show the state is per-video: `var emptyRun = 0; var skipsSince = 0;` sit at module scope. Falsifier fails -- the state is global as claimed. | CONFIRMED | Fix not applied (no keying by video element) per the row's own conditional FIX text ("only if a two-video page is reachable in smart mode") -- confirmed as a live-but-currently-inert defect instead. Read `dom.hasPlayerAncestor`: `isPlayer` tests `el.closest('#movie_player')`, a single DOM id a valid document can hold at most once -- so exactly one video element can ever read `isPlayer` true at a time on m.youtube, whose feed previews reuse THAT SAME element rather than minting a second one (see the "A FEED PREVIEW DURING A SCROLL" note in init-entry.js). A comment recording this scope and the escape hatch (key by video element, the way `delay-presenter.mjs` keys its graph, if a build ever attaches two independently player-tagged videos concurrently) was added directly above the module-global declarations. No behaviour change: the two-video precondition the FIX gates on is not reachable today. | `docs/critic/phase-i.md` I15, `app/gaze/src/person-skip.mjs`, `app/gaze/src/dom.js` |
| J1 | 2026-09-02 | phase-j (T3/T4) | EXPOSURE | The worker's VIDEO gender path fed faceres a NORMALIZED-square crop -- a 1.78:1 rectangle in pixels on 16:9, stretched to 224x224 (`worker-entry.js:336/351` and the in-page fallbacks `init-entry.js:2179/2526/2689` called `classifyFaceGenders` without `{square:true}`); the findings-16a squash defect, alive on the per-person path since 2026-08-24. Native squares in pixels, so every gender row of the first parity file measured the squash, not the port. | Add `{square:true}` at every video call site and re-run parity; predicted descriptor cosine p50 0.83 -> >= 0.98. | CONFIRMED | All five call sites carry `{ square: true }`; `native-wired.test.mjs` "every VIDEO gender crop is squared in pixels" walks both files and fails on any call without it (red against the previous source: 5 calls). Device re-run of `probe_native_parity.py` on the rebuilt APK is the check on the prediction -- see the plan log. | commit (this round), `test/native-wired.test.mjs` |
| J2 | 2026-09-02 | phase-j (T3) | WRONG-NUMBER | "native maxKp systematically LOWER" (plan log 16:00) is false: signed, native was higher on 3 of 16 frames and pearson between engines is -0.08; `maxKpAbsDiff` is absolute and cannot carry a sign. | Compute the signed diff over the banked file. | CONFIRMED | Withdrawn in the plan log; superseded entirely by J16 (the fp16 GPU delegate was blind on MoveNet, see arbiter rows) -- the second run reads native HIGHER on 11 of 16. | `native-parity-1788345089.json`, `native-parity-1788345674.json` |
| J3 | 2026-09-02 | phase-j (T3) | WRONG-NUMBER | The MoveNet parity gate "passed" as 0 == 0 on all 16 frames -- both arms admitted nobody, so `personIou.n` is 0 and no MoveNet parity was measured at all. | Read `personIou.n` in the banked file. | CONFIRMED | Corrected reading recorded; the fp32 run and the offline arbiter (`spikes/native/arbiter.{py,mjs}`, `native-frames-1788346009.json`) supply the MoveNet evidence the first file lacked. | `native-parity-1788345089.json` `personIou.n: 0` |
| J4 | 2026-09-02 | phase-j (T3) | WRONG-NUMBER | "5 of 24 gender label flips": 2 are null reads (descriptor magnitude ~1-3, under `NULL_MINT_NM_FLOOR` 5); at the shipped `GENDER_CLEAR_SCORE` 0.45 exactly 1 of 24 changes decision, in the covering direction. | Re-count against the shipped constants. | CONFIRMED | Corrected in the plan log; the gate for Task 5/8 counts decision flips at the shipped bars over solid-signal reads, not raw label agreement. | `native-parity-1788345089.json` |
| J5 | 2026-09-02 | phase-j (T3) | WRONG-NUMBER | Descriptor cosine 0.83 was attributed to three candidate mechanisms; (a) resampling of the MoveNet frame is real but is not the cosine cause, (b) colour/range is refuted (mad 0), (c) descriptor sensitivity is refuted (the two worst cosines are null reads). | Pixel probe. | CONFIRMED | `probe_native_pixels.py`: `A_vs_webgl` mad 0/0/0; crop geometry mad 0.2-0.4; the cosine gap is J1's crop stretch. | `native-pixels-1788345319.json`, `-1788345747.json` |
| J6 | 2026-09-02 | phase-j (T4) | EXPOSURE | `adoptNativePort` read `window.__TS_NATIVE_PORT` with no provenance check, so two lines of page script could hand the player path a fake engine that answers "no faces" on every frame -- silent fail-OPEN with every counter healthy, plus every frame and crop exfiltrated. | Page script sets the global and dispatches the event before the bundle boots; must NOT be adopted. | CONFIRMED | The stash keeps the port in its own closure and exposes a ONE-SHOT, non-configurable, non-writable `__TS_TAKE_NATIVE_PORT` at document start (before any page script); the bundle reads the port from nowhere else (`native-wired.test.mjs` asserts no `window.__TS_NATIVE_PORT` read remains, red against the previous source); `native-client.mjs` ignores any port MessageEvent with `isTrusted === false`, so a script holding the real port cannot forge replies either (`native-client.test.mjs` "a MessageEvent the page dispatched on the port is ignored"). lib.rs test pins the defineProperty shape. | commit (this round) |
| J7 | 2026-09-02 | phase-j (T4) | EXPOSURE | A cid minted on one engine could be handed to the other mid-pass (`vid()` re-read per call across an await), and a dead native client never swept its held full-resolution bitmaps (`sweepCrops` runs only from `cropFaces`). | Stub client dies between cropFaces and cropGender. | CONFIRMED | Each crop chain binds `vid()` once (`eng` / `zeng`); `die()` closes and clears every held crop (`native-client.test.mjs` "die() closes every crop the client still holds", red before: bitmap stayed open). | commit (this round) |
| J8 | 2026-09-02 | phase-j (T4) | WRONG-NUMBER | The spend budget took `waitMs()` baseline from one engine and the delta from another if the engine changed mid-pass; a worker-minus-native subtraction reads a large positive and charges the pass nothing. | Swap engines between the two reads. | CONFIRMED | `waitEng` captured once per pass, both reads on it; `player-budget.test.mjs` re-pointed and asserts no `vid().waitMs()` remains. | commit (this round) |
| J9 | 2026-09-02 | phase-j (T4) | WRONG-NUMBER | `nativePasses` counted INTENT (bumped before the request, never reversed): it read 2 on the run where zero replies ever arrived. Task 5's gate also named `nativeFallback`, a counter Task 4 never built. | Read the broken run's counters. | CONFIRMED | `nativePasses` bumps on the RESOLVED videoFrame; `nativeReplies` / `nativeErrors` bump inside the transport's own `noteSuccess` / `noteFailure` (`onReply` hook); the Task 5 gate reads `nativeErrors` / `nativeDead`. | `native-port-1788344212.json` (`nativePasses 2`, replies 0); commit (this round) |
| J10 | 2026-09-02 | phase-j (T4/T5) | EXPOSURE | On 1 of 2 "native live" device runs the WebGL worker was dead for the ENTIRE run (`loadFailed:gender` at 3.0s while Kotlin compiled GPU kernels), so the fallback the architecture's safety argument rests on did not exist; `workerVideo()` returns true whenever native is live, hiding it from every in-page readiness check. | 5 native runs on the Redmi reporting `workerDead`; force the native client dead and confirm subjects stay covered. | CONFIRMED | On the pull-port build (88d6957): worker `webgl`, dead false on 8 of 8 native runs -- 4 port navigations (+5 reloads), A/B native1/native2/native-off, and the fail-safe probe's pre-read. The 1-of-2 death was on the PUSHED-port build where Kotlin compiled kernels under the worker's model load; not reproduced since. Fail-safe measured directly (`probe_native_failsafe.py`, `native-failsafe-1788352884.json`): native client terminated under a playing video at t=224 -- `nativeDead` 0 -> 1 once, `nativePasses` flat 83 for 40s (the +1 is the in-flight pass resolving; the probe's verdict compared across the kill and was corrected to compare inside the after-window), worker webgl alive, verdict passes 51 -> 28 per 40s, covered samples 15/20 -> 12/20. No exposure: the worker took the player over on the first pass. | `native-port-17883472{15,98}.json`, `native-port-1788347381.json`, `latency-ab-native{1,2,-off}.json`, `native-failsafe-1788352884.json` |
| J11 | 2026-09-02 | phase-j (T4) | NIT | Both model sets are resident (tfjs in the worker for images + TFLite in Kotlin) and no RSS number exists for it on the Redmi. | `dumpsys meminfo` during a native run. | CONFIRMED | `probe_native_meminfo.py` (`native-meminfo-1788348521.json`), same build, same video at t=217+60s: native ON TOTAL PSS 416,179 KB (RSS 388,076, Graphics 158,828, Native Heap 38,026, Java 10,076); kill-switch NATIVE_INFER 0 PSS 453,587 KB (Graphics 187,444, Native Heap 42,547). Native ON is 37 MB SMALLER, 29 MB of it Graphics: the WebGL worker's video-path textures cost more than the TFLite engine's buffers. HONEST LIMIT: both arms have the engine resident (`nativeReady 1` in the OFF arm), so the residency cost of the TFLite models themselves is NOT isolated -- that needs a build without `NativeInfer`, and the A/B says it is bounded by 37 MB in the covering direction. | `native-meminfo-1788348521.json` |
| J12 | 2026-09-02 | phase-j (T2) | NIT | Kotlin thread-safety concern. | Read `NativeInfer.kt`: everything serialises on one `HandlerThread`. | REFUTED | A reply cannot cross a `bind()` because `run()` is synchronous on the same handler; a future async `run` would break this silently -- noted in the Kotlin. | `NativeInfer.kt` |
| J13 | 2026-09-02 | phase-j (T4) | NIT | `NATIVE_INFER: 1` in `rules/tuning.json` reaches 1092 phones over OTA before the release. | Read `tuning.mjs`: unknown keys are refused, so it is inert on 1092. | REFUTED | Verified by reading the apply path; a test already pins unknown-key refusal. | `tuning.mjs` |
| J14 | 2026-09-02 | phase-j (T3) | NIT | The parity probe's `boxesFrom` switched engines per frame, n=24 against the requant precedent's 100, and count-mismatch frames were dropped from the IoU denominator. | -- | OPEN | Probe still uses the worker's boxes when it has them; n grows with the reruns (the phase-j rerun `native-parity-1788347487.json` is 16 frames / 24 reads, still a quarter of the requant precedent); recorded as a limit of the instrument, not a number. Non-blocking: no shipped figure rests on it. | `probe_native_parity.py`, `native-parity-1788347487.json` |
| J15 | 2026-09-02 | phase-j (T3) | WRONG-NUMBER | The native MoveNet input differed from `tf.image.resizeBilinear` by mad 4.8-5.7 levels (max 217); the mechanism is the half-pixel centre convention, not the filter: the shifted source rect reads mad 0.04 against 4.82 for `imageSmoothingQuality: high`. | Recipe sweep on the device. | CONFIRMED | `native-client.mjs` `drawTo` draws the source rect shifted by `(scale-1)/2` with 'low' smoothing; `native-client.test.mjs` "the whole-frame squash samples where tf.image.resizeBilinear samples" is red against the plain draw (9/9 -> 8/9). Arbiter shows the resize moves MoveNet maxKp by only ~0.01 (0.768 -> 0.779), so it was never the blindness. | `native-pixels-1788345747.json` |
| J16 | 2026-09-02 | phase-j (T3) | EXPOSURE | Native fp32 MoveNet admits people on 11 of 16 frames where the WebGL worker admits NOBODY on all 16; the "twelve slots n:0" regime attributed to the DEVICE for six loops is a RUNTIME defect, and the parity gate as written would block the fix by requiring native to reproduce the blindness. The critic's live mechanism (the resize) is REFUTED by the arbiter. | Dump the 256 frames off the device and run the SAME model on three runtimes none of the device paths use. | CONFIRMED | `spikes/native/arbiter.py` (TFLite CPU f32 and f16) and `arbiter.mjs` (tfjs CPU) agree with native-GPU-fp32 to 2 decimals (t=60 maxKp 0.768/0.779, 2 admitted; t=217 0.822, 1 admitted; t=300 0.17, 0) where the phone's tfjs-WebGL read 0.187 / 0.033 / 0.073 and admitted nobody, and the TFLite GPU delegate at fp16 read lower still. The tfjs WebGL runtime on Adreno 610 is blind to MoveNet on this footage; the fp16 GPU delegate is too. Fixed at source: `setPrecisionLossAllowed(false)` (frame 288 -> 350ms). Findings 36 / 21 / 21a / 23 / `CUT_PERSON_LOOK` / `PERSON_LETTERBOX` were priced in a regime the phone was never in -- recorded in `docs/engine-findings.md` section 25; each is re-opened, not re-priced here. | `native-frames-1788346009.json`, `native-parity-1788345674.json`, `native-parity-1788345089.json` |
| K1 | 2026-09-02 | phase-k (3) | EXPOSURE | Per-model fp16 shipped on BlazeFace, and on the one frame of the parity corpus where MoveNet admits nobody AND a face exists, fp16 LOSES the face. t=90: fp32 native 1 face (conf 0.455), worker 1 face (conf 0.459), fp16 native **0 faces**, `persons: []` in both arms. That face is the frame's entire cover — the new bench scores t=90 at worker 0.686 / native 0.686 from ONE synthetic body — so on fp16 a close-up filling 0.60 x 0.60 of the frame has no patch at all. The round's claim answers the GENDER labels (22/24) and never mentions the lost DETECTION; the summary records it only as `faceCountMismatchFrames: 1`. | Re-dump parity 3x per arm and read t=90's `native.faces` length. If fp16 finds the face on any repeat, this is the known non-determinism (findings 16a) — and then the parity dump cannot establish fp16 face parity at all, which is the same problem. | CONFIRMED | FIXED AT SOURCE: `NativeInfer.kt` `MODEL_FP16 = setOf(2)` -- BlazeFace back to fp32, faceres stays fp16 (gender raw diff p50 0.0025, cosine min 0.995). The 1094 parity rerun must read faceCountMismatchFrames 0; artifact in the 1094 plan-log row. 2 of 16 frames (t=90, t=300) are face-only; fp16 loses one of them. Cheapest fix if it reproduces: keep BlazeFace (model id 1) in fp32 and leave only faceres at fp16 — the measured gender saving is 220 -> 182ms, the frame saving only 359 -> 345ms. | `native-parity-1788347487.json`, `native-parity-1788351287.json` |
| K2 | 2026-09-02 | phase-k (5) | WRONG-NUMBER | Findings 26's snapshot table reverses under time weighting, and the reversed version reproduces the coverage drop it was written to explain away. The table is UNWEIGHTED per-verdict counts, but the two arms sample at snapshot gap p50 **0.801s (native)** against **2.034s (worker)** — 2.5x apart. Weighting each snapshot by the media time to the next: native has a blurred track **0.643** of the time, worker **0.673**. Native covers LESS, not more, by 0.030 — against the frame-level `coverage` difference of 0.021 (0.583 vs 0.604) in the same two files. So "At verdict time native covers more often" is a sampling artifact and "the frame-level drop lives BETWEEN verdicts" does not follow from it. | Recompute the table weighting each snapshot by `mt[i+1] - mt[i]`; if the sign holds, this is refuted. | CONFIRMED | CONFIRMED by recomputation (native 0.643 / worker 0.673 time-weighted). Findings 26 rewritten: the snapshot table is recorded as a sampling artifact and the attribution rests on the geometry bench and the exit hang alone, stated as one instrument. The unweighted numbers reproduce exactly (0.363 / 0.769 / 0.328 vs 0.421 / 0.724 / 0.282), so the arithmetic is right and the inference is not. The exit-hang half of findings 26 (native p50 3, worker p50 58) is untouched by this and still supports the phantom story — it just no longer has a second instrument agreeing with it. | `latency-ab-native-fp16.json`, `latency-ab-native-fp16-off.json` `delayArm.snaps` |
| K3 | 2026-09-02 | phase-k (1) | WRONG-NUMBER | The yield gate's SHIPPED comment claims a number the round's own data refutes, and findings 26 says the opposite thirty lines away. `init-entry.js`: "One position pass of tracking traded for ~250ms off every verdict gap". Measured, yield-only arms against the 1093 baseline: gap p50 1213 -> **1189 / 1192**, i.e. **24ms (1.9%)**, while positions fell **78 -> 53 / 58 (-26 to -32%)**. Findings 26 states this correctly ("did not move the gap (1189) ... the slot was the loss, not the collision") and the source comment was not updated. `test/position-yield.test.mjs` repeats the same wrong rationale in its header. | `python` over the four banked runs' `verdictGapP50` and `positions`. | CONFIRMED | FIXED: the yield gate is REMOVED from `init-entry.js` (with its comment) and from `position-yield.test.mjs`; the hoist and its test stay. Findings 26 carries the measured 24ms / -26-32% positions. The gate still ships. It is not harmful in the shipped config (verdicts + positions per 150s: 1093 180, clock 202-211), but a shipped comment quoting 10x the measured benefit is how the next round re-derives a wrong trade. | `latency-ab-native1093.json`, `-yield.json`, `-yield2.json` |
| K4 | 2026-09-02 | phase-k (4) | SCOPE | `CUT_PERSON_LOOK` 1 was priced on a cadence that no longer ships. `latency-ab-native-cutlook.json` carries `bundle 75cddef` — the 1093 release, BEFORE the clock hoist in this same commit. On that build the +15% (gap 1213 -> 1399) is right. The build the default now ships in reads gap 800, verdicts 148-152 against 102, and MoveNet skipped on **31-33% of passes (63-69 of 202-211)** against **20% (36 of 180)**. The forced look fires once per cut (30-31 cuts / 150s) against a back-off that is engaged on a third of passes instead of a fifth, so its cost on the shipping clock is unmeasured. | Re-run `probe_latency_ab.py --delay` with `CUT_PERSON_LOOK` 0 and 1 on the hoisted build and compare the gap. | CONFIRMED | CONFIRMED; re-priced on the 1094 clock (default 1 over OTA against a planted 0), numbers in the 1094 plan-log row; the `coverage 0.55 -> 0.616` sentence in `person-skip.mjs` is now marked as inside the spread. The `~5s` exposure argument HOLDS at both cadences (see the clean list). What does not carry is the price. Also: the comment's "coverage 0.55 -> 0.616" is inside same-config run-to-run spread — `native-yield` 0.651 vs `native-yield2` 0.592 is 0.059 on identical settings. | `latency-ab-native-cutlook.json` (`bundle`, `tuning`), `latency-ab-native-clock2.json` |
| K5 | 2026-09-02 | phase-k (6) | WRONG-NUMBER | Ledger J10 is closed citing the artifact whose OWN verdict field says the probe FAILED, and the coverage numbers it quotes reverse on the corrected run. `native-failsafe-1788348270.json` `verdict` = **"nativePasses kept rising after the kill (82 -> 83)"**, and J10 quotes "covered samples 15/20 -> 12/20" from that file. The corrected run `native-failsafe-1788352884.json` reads **"FAIL-SAFE HOLDS ... covered samples 12/20 -> 16/20"** — opposite direction, same n=20. And the "+1 is the in-flight pass resolving" argument does not survive it: in the corrected run `nativePasses` crosses the kill **85 -> 90 (+5)** with replies 288 -> 294, because the kill lands between two 5s samples. | Read `verdict` and `before.rows[-1]` / `after.rows[0]` in both failsafe files. | CONFIRMED | FIXED: `probe_native_failsafe.py` reads the counters immediately before `terminate()` (`atKill`) and fails if the after-window exceeds that read by more than the one in-flight pass; J10 re-cited to the passing artifact `native-failsafe-1788352884.json` and the 1094 rerun. THE FAIL-SAFE ITSELF HOLDS and I am not disputing it: both after-windows are flat (`nativePasses` 83 x 20 and 90 x 20, `nativeReplies` 240 x 20 and 294 x 20), `workerDead` false throughout, patches return within one sample. What is wrong is the closure's evidence pointer and the +1 reasoning. Cite 1788352884 and drop the +1 sentence. | `native-failsafe-1788348270.json`, `native-failsafe-1788352884.json` |
| K6 | 2026-09-02 | phase-k (1/2) | NIT | The hoist roughly DOUBLES the share of verdict passes thrown away, and no row records it. `passDropped` 9 -> **18 / 19**; dropped verdicts 5 of 102 (4.7%) -> **14 of 148 (9.5%)** and **17 of 152 (10.1%)**. A verdict that may start on any 120ms tick is likelier to be in flight when a cut lands, and `myEpoch !== passEpoch` discards it. | Compare `verdictsDropped / verdicts` and `lifeDelta.passDropped` across the arms. | CONFIRMED | CONFIRMED, recorded in findings 26 and the plan log (dropped share 4.7% -> 9.5-10.1%); the net (`secsPerVerdictUseful` 1.47 -> 1.01) stands. Not changed: a pass a cut invalidated is rightly dropped. Net still a large win (`secsPerVerdictUseful` 1.47 -> 1.01), which is why this is a NIT and not a number that changes the decision — but the round's table quotes only the p50 gap, and a doubled waste rate is GPU spend on the phone the whole project is cadence-bound on. | `latency-ab-native1093.json`, `-clock.json`, `-clock2.json` |
| K7 | 2026-09-02 | phase-k (2) | NIT | "1213 -> 800" is not all scheduling, and 800 is the duty dial. Verdict cost p50 fell **474 -> 381 / 355** between the arms, so `effZoom = max(400, cost * VERDICT_DUTY)` fell 948 -> 762 / 710 on its own. Slack above effZoom: **265ms at 1093, 40ms at clock** — so ~225ms of the 413ms is the hoist and ~186ms is the cheaper verdict, itself downstream of MoveNet being skipped on a third of passes instead of a fifth (K4). The new p50 gap now tracks effZoom within 40-90ms: **the binding constraint is `VERDICT_DUTY`, not the scheduler.** | Bank `lastVerdictMs` per verdict and regress the gap on it; or run `VERDICT_DUTY` 1.5 on the hoisted build (`duty15` was run on 75cddef, the old clock). | CONFIRMED | CONFIRMED, recorded in findings 26: ~190ms of the 413 is cost shrinking through `effZoom`; the duty dial is binding on the hoisted clock. `VERDICT_DUTY` stays 2; 1.5 is one OTA push and is NOT pushed unmeasured on the new clock. Consequence, and it is a free win nobody has taken: on the hoisted clock `VERDICT_DUTY` 1.5 should read a gap near 600, over OTA, no install. It is also ~25% more `nativePasses` of GPU. | `latency-ab-native*.json` `verdictMsP50` / `verdictGapP50`, `latency-ab-native-duty15.json` `bundle` |
| K8 | 2026-09-02 | phase-k (1) | NIT | The rewind the new test calls "the line that matters" cannot do what its comment says. `lastSample = now + verdictDueIn - effInterval` is defended as "without it the yield would push the verdict out by a whole effInterval instead of pulling it in" — but the gate added in the SAME commit is `if (now - lastSample < effInterval && !verdictDue) return;`, so a due verdict passes it whatever `lastSample` holds. The rewind cannot move the verdict, and the verdict overwrites `lastSample = now` when it fires. Its only reachable effect is when the verdict is BLOCKED (`overBudget`, `inputPending`, `verdictBusy`): there it lets a POSITION pass run earlier than `effInterval` — the opposite of yielding. | Delete the rewind line and re-run the A/B; the gap must not move. | CONFIRMED | FIXED with K3 -- the rewind and its rationale are gone. The regex in `position-yield.test.mjs` is not vacuous (deleting the block fails it) but it pins the rewind on a rationale that is false, which is the shape that produced G1/G5/G9. | `app/gaze/src/init-entry.js` (the `verdictDueIn` block), `app/gaze/test/position-yield.test.mjs` |
| K9 | 2026-09-02 | phase-k (4) | NIT | `person-skip.test.mjs` cites a test file that does not exist: "rules/tuning.json must agree (tuning-json.test.mjs pins that)". There is no `test/tuning-json.test.mjs`. | `ls app/gaze/test/` and grep for `tun`. | CONFIRMED | FIXED: `person-skip.test.mjs` and `person-skip.mjs` cite `tuning.test.mjs` (the value pin at :174). The pin is REAL, only misnamed: `test/tuning.test.mjs:174` "the shipped tuning.json equals the shipped constants exactly" reads the VALUE (`assert.equal` of `obj[k]` against the module constant), not the key, and `rules/manifest.json` was regenerated correctly (tuning.json sha256 `af9eb399...` matches the LF-normalised file). Fix the citation. | `app/gaze/test/tuning.test.mjs:174`, `rules/manifest.json` |
| K10 | 2026-09-02 | phase-k (1) | NIT | `delayVerdictLate` read **214** on `native-clock` and **0** on `native-clock2` — identical config, same video, same seek. The handoff already carries this counter as an unexplained open instrument; the round did not record that it went from 0 on every 1093 arm to 214 on one arm of the shipping change. | Bank it per verdict with the presented media time and the `boxesAt` inputs. | CONFIRMED | CONFIRMED as an open instrument: `delayVerdictLate` 214 vs 0 on identical arms and `delayVerdictLateFrac` 0.0 in both. Recorded in the plan log; not quoted anywhere. `delayVerdictLateFrac` reads 0.0 in every `delayArm` block, so the two halves of the same instrument disagree. Diagnostic only, not user-visible; do not quote either number until it is explained. | `latency-ab-native-clock.json` `lifeDelta`, `latency-ab-native-clock2.json` |
